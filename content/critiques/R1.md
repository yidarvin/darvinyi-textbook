verdict: approve

## Critique round 1

Independent re-derivation of commit `96a1e19` ("build: R1 -- ch19 GAN widget fidelity") against `CRITIQUE.md`'s B-01, B-02, and the ch19 rows of M-01, and `context/STYLE_GUIDE.md`'s widget-fidelity rule.

### Scope check
`git show 96a1e19 --stat` touches exactly `prompts/queue.md` and six ch19 files (`CycleConsistency.jsx`, `DBEvolution.jsx`, `MinimaxGame.jsx`, `ModeCollapse.jsx`, `TrainingDynamics.jsx`, `src/pages/ch19/GANs.jsx`). No scope creep. (Note: the working tree separately has *uncommitted* diffs to `src/components/diagrams/ch21/BenchmarkSaturation.jsx` and `src/pages/ch20/DiffusionModels.jsx` — these correspond to future queue items R3/R4's B-03/M-04 fixes, are not part of the R1 commit, and are out of this critique's scope; noted for the record only, not treated as an R1 finding.)

### MinimaxGame.jsx (B-01) — verified real
`getD(x, muG, sigmaG)` is now the literal density ratio `p_data/(p_data+p_G)`, recomputed from the live `muG`/`sigmaG` args — confirmed by reading the render path (`trajAt(ds)` feeds `muG/sigmaG` into `getD` every frame at :253, :315, :456-457). `computeJS` is an honest JS-divergence Riemann sum. `computeTrajectory` performs real RMSProp gradient descent (finite-difference gradients of `computeJS`, decayed second-moment, epsilon-stabilized update) starting from `muG=-1.0, sigmaG=1.5` for 20 steps. I independently re-executed this exact algorithm in Node: it converges to `muG≈1.479, sigmaG≈0.702, JS≈0.00011` at step 20 — matching the real data params (`1.5, 0.7`) and the commit message's claimed `D=0.50/0.50` result. `isNash = jsd < 0.01` only fires once the numerically-derived divergence genuinely drops below threshold (first at step 18); the "Nash equilibrium" label is earned, not scripted. No trace of the old step-indexed sigmoid or linear ramp remains.

### DBEvolution.jsx (B-02) — verified real
`grep -n "D_ACCURACY"` returns nothing. `WEIGHTS` is now `trainDiscriminatorWeights(REAL_PTS, ALL_FAKE)`, real full-batch gradient descent (300 steps, L2-regularized logistic regression) run to convergence against each snapshot's fake cloud — I re-implemented and re-ran this in Node and reproduced the same non-monotone accuracy sequence the widget computes: 78.3% → 80.0% → 66.7% → 63.3% → 60.0% → 61.7% at the six saved epochs. `dAccPct` in the component is computed live via `discAccuracy(w, REAL_PTS, fakePts)` on the interpolated weights/points — genuinely reactive, not a lookup array. The reconciled prose in `GANs.jsx` ("starts around 78%... trends down toward roughly 60% by epoch 20... never quite reaches 50%") matches these recomputed numbers exactly. The fake point clouds (`ALL_FAKE`) remain hand-authored stand-ins for a generator's output — disclosed in a code comment (:153-157) and implicitly covered by the prose's honest "the discriminator is really retrained... genuine (if noisy) trained accuracy, not a scripted curve" framing, which never overclaims that the generator itself was trained live. This satisfies the R-02 accept bar (`D_ACCURACY` gone, accuracy live-derived).

### TrainingDynamics.jsx (M-01) — verified disclosed
Added the exact-style disclosure block (:429-434): "Illustrative, not measured: these are representative curve shapes for three well-documented GAN failure modes, not a recorded training run — only the ln(2) equilibrium value and the D/G loss formulas it anchors to are drawn from the actual minimax objective." Matches the `ResidualStream.jsx`/`SkipConnection.jsx` pattern in wording and placement.

### CycleConsistency.jsx (M-01) — verified disclosed + conceptual error fixed
The invented numeric `‖F(G(x))−x‖₁` readout is gone; the widget now shows a qualitative `Consistent`/`Broken` state driven by the `brokenCycle` toggle, with a disclosure block (:494-501) that also explicitly corrects the λ-coupling error the critique flagged: "λ affects how a model is trained, not what a fixed G/F pair outputs on one forward pass." The λ slider now only updates the displayed loss formula, not the diagram. Prose in `GANs.jsx` (:731-745) was reconciled to match — no stale numeric cycle-loss value remains anywhere in the file.

### ModeCollapse.jsx (M-01, and the Minor GPQA-adjacent ModeCollapse finding) — verified real, math checked
`toyFID` implements the standard formula `‖μ_r−μ_g‖² + Tr(Σ_r+Σ_g−2·sqrtm(Σ_rΣ_g))` exactly (`meanCov` computes real sample mean/covariance; `traceSqrtProduct` computes `Tr(sqrtm(AB))` via the closed-form 2×2 eigenvalue trick). I checked the math: for two PSD matrices A, B, AB is similar to the symmetric PSD matrix `A^{1/2}BA^{1/2}` (via conjugation by `A^{1/2}`), so its eigenvalues are real and non-negative, making `Tr(sqrtm(AB)) = √λ₁+√λ₂` with `λ₁,λ₂` the roots of `λ²−tr(AB)λ+det(AB)=0` — exactly what the code computes, with a `Math.max(0, disc)` guard for float noise. I re-implemented `generateAll`/`toyFID` independently in Node and got `healthy≈0.0122, collapse≈9.11, wgan≈0.032` — matching the reconciled prose in `GANs.jsx` ("from about 0.01 to about 9" for collapse, "≈0.01... ≈9... ≈0.03" in the `tryThis.notice`) to the stated precision. The in-widget disclosure ("a real statistic, but a toy stand-in for FID's actual feature space") is present and accurate.

### Prose reconciliation
Grepped `GANs.jsx` for the old fabricated values (`92%`, `51%`, `~84`, `~12`, `D_ACCURACY`) — no hits. The "Nash equilibrium" and "flattens toward 0.5" language in the MinimaxGame hand-off is now true of the recomputed trajectory (verified above), and the chapter's own "D never actually reaches D*" caveat (:222-224) is preserved, so the widget isn't overclaiming realism it doesn't have.

### Gate
`npm run check` (vite build + Playwright 28-route smoke): **28/28 passed**, build clean.

### Verdict rationale
All three build items (B-01, B-02, and the ch19 M-01 rows for `TrainingDynamics`, `CycleConsistency`, `ModeCollapse`) are genuinely fixed per the standard `STYLE_GUIDE.md:40` sets: real computation where claimed, honest disclosure where a real computation is out of scope. No REQUIRED findings.

ADVISORY (non-blocking): `DBEvolution`'s title still reads "20 epochs of adversarial training," which is slightly generous — only the discriminator is genuinely retrained per snapshot; the generator's fake-point clouds remain hand-authored (disclosed in a code comment, not in the rendered UI). The reconciled prose is honest about the mechanism and doesn't overclaim, so this doesn't rise to a fidelity violation, but a future pass could either soften the title or surface the "generator output is an authored stand-in" caveat in the rendered widget copy itself rather than only in source comments.
