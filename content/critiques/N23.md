verdict: approve

## Round 1 review (2026-07-15)

Independent review: read `AGENTS.md`, the N23 queue row, Appendix B, Appendix C's N23 specification, the V2 editorial standard and curriculum, the N23 build commit, and every current N23 page, diagram, widget, routing, and navigation source line. No prior N23 critique history exists. Re-derived all displayed computations and exercised every widget formula across its complete control range. The ridge probe gives layer accuracies 87.5%, 100%, 100%, 100%, 100%, and 100%; the default SAE code is `z = [0, 0, 0.9317, 0]` with reconstruction `[0.6708, 0.6708]` and error `0.1605`; and the default warmth steering state `[1.05, 0.47]` produces logits `[0.8455, 1.3900, 0.1260]` and probabilities `[31.15%, 53.69%, 15.17%]`. The symbolic induction trace is consistent with the previous-token/induction-head construction it discloses. `npm run check` passes; Vite still emits the repository's existing unresolved font and KaTeX asset warnings. Per the user's instruction, I did not launch a preview. Checked the factual claims against the primary Alain and Bengio probe paper; Elhage et al. superposition article; Olsson et al. induction-head article; Bricken et al. and Templeton et al. SAE articles; Turner et al. activation-engineering paper; Cunningham et al. SAE paper; and Anthropic's 2025 circuit-tracing publication.

## Required fixes

1. **`SuperpositionExplorer.jsx:6-21` contradicts the intended “more features than dimensions” lesson at valid control values.** The full-circle construction uses `2πi/count`, so at `count = 2` it creates antipodal rather than independent directions: `f1 = (0,-1)`, `f2 = (0,1)`, `h = f1 + 0.65f2 = (0,-0.35)`, and the widget reports the positively active second feature as `r2 = -0.35`. At every even count it also contains antipodal pairs and therefore reports maximum pair coherence `1.00`; the mean inactive cross-talk is not monotone (`0.825, 0.825, 0.720, 0.706, 0.800, 0.858, 0.859` for counts 3 through 9). This false boundary behavior undercuts the page's claim that interference is the cost that appears when feature count exceeds dimension. Use a geometry in which two features can be orthogonal and added features progressively crowd the available directions (for example, evenly spaced projective directions over a half-circle), then make the readout and directed observation agree with the recalculated values.
2. **`Interpretability.jsx` has no worked micro-example with concrete numbers.** Appendix C requires the Editorial Standard throughout, and that standard requires one worked numerical example per chapter. Exercise 02 asks the reader to do a calculation, but the chapter never works one through. Add a short in-body example that substitutes numbers into one of the chapter's equations and reaches a displayed result. The steering widget's disclosed default is a cheap consistency target: `h = (0.25,0.35)`, `v = (1,0.15)`, and `α = 0.8` give `h' = (1.05,0.47)` and softmax probabilities `31.15%`, `53.69%`, and `15.17%` under the existing decoder.
3. **`Interpretability.jsx:17-23,58-80` does not deliver Appendix C's 2024–26 interpretability update.** Every cited circuit/steering source is from 2022–2023 and the newest SAE source is from 2024; the only frontier update after induction heads is the 2024 Claude 3 Sonnet SAE result. The named purpose is to extend Chapter 10's 2022 seed to the 2024–26 story. Add a concise, sourced account of the major post-2024 move from isolated features to feature-level computational graphs, at minimum Lindsey et al.'s 2025 *Circuit Tracing: Revealing Computational Graphs in Language Models* (cross-layer replacement model, attribution graphs, and its explicitly reported partial-coverage/tool-artifact limitations). A brief 2025 steering update such as persona vectors would also bring the final section's evidence beyond ActAdd, but the required core is current circuit-level coverage rather than another catalog item.

## Advisories

- `ProbingPlayground.jsx:43-51` fits and scores the probe on the same eight points. The copy discloses the fit set, and the arithmetic is correct, so this does not block; label the metric “training accuracy” or use a held-out/cross-validated score so the chapter's “decodable” language cannot be mistaken for evidence of generalization. Leave-one-out accuracy for the existing points is 75% at layer 1 and 100% at layers 2–6.
- `Interpretability.jsx:69` says Bricken et al. “introduced” dictionary learning for transformer activations. Their article explicitly says it builds on prior dictionary-learning work and notes Cunningham et al.'s earlier 2023 SAE manuscript. “Demonstrated the approach on a one-layer transformer” would avoid an unintended priority claim, or add Cunningham et al. to the references.
- `Interpretability.jsx:45-47` places the evidence diagram between the probe hand-off and the probe itself, despite the Editorial Standard's rule that the directed sentence immediately precede its widget. Move the probe-specific imperative below the diagram.
- `Interpretability.jsx:76-79` changes the steering-strength symbol from `α` in the chapter equation to `λ` in the widget. The computation is unchanged, but one symbol would reduce needless notation drift.
- `MechanisticEvidenceLadder.jsx:3-7` puts “circuit hypothesis” in a column headed “OBSERVATION.” Relabel that column to “QUESTION OR TEST” or make the circuit row an actual intervention/measurement so the diagram does not visually promote a hypothesis into evidence.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED finding from Round 1 against the current chapter and widget. N23 remains `DRAFT` in `prompts/queue.md`.

1. `src/components/widgets/ch23/SuperpositionExplorer.jsx` now places feature directions at evenly spaced projective angles over a half-open half-circle. At two features the directions are orthogonal and both active readouts are positive; added features progressively raise nearest-pair coherence rather than introducing antipodal duplicates. The active-pair selector is bounded to adjacent, non-wrapping features, its count-change state is clamped to that range, and the widget's geometry/readout labels now disclose the recalculated quantities.
2. `src/pages/ch23/Interpretability.jsx` now includes a worked activation-steering example: substituting `h=(0.25,0.35)`, `v=(1,0.15)`, and `α=0.8` yields `h′=(1.05,0.47)`, followed by the frozen toy decoder's stated logits and softmax probabilities.
3. `src/pages/ch23/Interpretability.jsx` now cites Ameisen et al. (2025), *Circuit Tracing: Revealing Computational Graphs in Language Models*, and explains cross-layer-transcoder replacement models, prompt-specific attribution graphs, and their bounded coverage and tool limitations.

No advisories were taken because this resolution is scoped to the Round 1 REQUIRED findings. `npm run check` passes (with the repository's existing unresolved font and KaTeX asset warnings).

## Round 2 review (2026-07-15)

Independent re-review: read the complete Round 1 history and builder resolution, `src/pages/ch23/Interpretability.jsx`, and `src/components/widgets/ch23/SuperpositionExplorer.jsx`; inspected the frozen decoder used by the worked example; re-derived the projective geometry and readouts for every feature count and active-pair position; and checked the new circuit-tracing paragraph against Ameisen et al. (2025), *Circuit Tracing: Revealing Computational Graphs in Language Models*. `npm run check` passes, with the repository's existing unresolved font and KaTeX asset warnings. Per instruction, no preview was launched.

Regression gate: all three Round 1 REQUIRED findings hold. The half-open projective construction gives orthogonal directions and positive active readouts at two features, then raises nearest-pair coherence through `0.92` at eight features without antipodal duplicates; the displayed dot products and mean inactive cross-talk are computed from the same encoded vector. The in-body example correctly obtains `h'=(1.05,0.47)`, logits `(0.8455,1.3900,0.1260)`, and softmax probabilities `(31.15%,53.69%,15.17%)`. The 2025 coverage accurately describes the cross-layer-transcoder replacement model, prompt-specific attribution graphs, and the source's reconstruction, pruning, missing-QK-circuit, and mechanistic-faithfulness limitations. No REQUIRED issue remains, so N23 is materially truthful and approved.

## Advisories

- Carried from Round 1: `ProbingPlayground.jsx:43-51` reports fit-set accuracy. Labeling it “training accuracy” or using a held-out score would make the scope clearer.
- Carried from Round 1: `Interpretability.jsx:71` says Bricken et al. “introduced” the dictionary-learning approach despite prior transformer-activation SAE work. “Demonstrated the approach on a one-layer transformer” would avoid the priority implication.
- Carried from Round 1: `Interpretability.jsx:46-48` still places the evidence diagram between the combined hand-off and the probe widget. A probe-specific imperative immediately before the widget would follow the editorial skeleton more literally.
- Carried from Round 1: `Interpretability.jsx:78-84` and `SteeringDemo.jsx` use `alpha` in the chapter and `lambda` in the widget for the same steering strength.
- Carried from Round 1: `MechanisticEvidenceLadder.jsx` still places “circuit hypothesis” beneath the heading “OBSERVATION”; “QUESTION OR TEST” would be more precise.
