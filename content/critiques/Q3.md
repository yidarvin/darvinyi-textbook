verdict: resolved

## Critique round 1 (2026-07-15)

Independent review: read `AGENTS.md`, the full queue and Q3 specification, the relevant Phase Q and Appendix B material in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, both cumulative Q3 commits (`2445d64`, `3c08ed8`), and every current source artifact changed by Q3. No prior Q3 critique history exists. Re-derived the 375px content width from the current responsive tokens, traced every changed layout and interaction path, and ran `npm run check` (passes with the repository's existing unresolved font and KaTeX asset warnings). The in-app browser exposed no available backend after its prescribed recovery check, so this round does not claim rendered desktop, 375px, touch, interaction, equation, or console observation. Q3 changes no textbook factual claims or citations, so no subject-matter primary-source check applies; touch-input portability was checked against the W3C Pointer Events specification. The new opt-in stacking rules, the MDP canvas coordinate conversion, the 4-column SPADE palette, and most marked stat strips are sound improvements, but the item does not yet satisfy its named worst-offender scope.

### REQUIRED

1. **`RLHFPipeline.jsx:124`, `SPADESynthesis.jsx:274-338`, and `MultiAgent.jsx:21-27,623-629` still shrink dense multi-column SVGs into unreadable phone diagrams instead of stacking them or preserving a fixed readable width.** At a 375px viewport, `src/index.css` leaves about 317px inside the widget body (`375 - 2*16px` chapter padding, then roughly `2*12px` widget-body padding). These `580`, `560`, and `616` unit diagrams therefore render at scale factors of about `0.55`, `0.57`, and `0.51`. Their source labels of `7.5–12px` become approximately `4–6.6px`, including the RLHF input/process/output columns, the SPADE normalization paths, and all four Multi-Agent role boxes. The new `data-mobile-stack` attributes only stack neighboring detail/stats panels; they do not repair these named multi-panel diagrams. Q3 explicitly requires the RL/agents multi-panel offenders and SPADE successor to stack below the breakpoint or use a fixed `ScrollableFigure`. Give each diagram a mobile-specific stacked representation, or preserve a readable minimum width in an explicit horizontal scroller instead of scaling the complete desktop layout to 317px.
2. **`ScalingLaws.jsx:271-272,445-467` and `src/index.css:169-179` turn every `StatDivider` into an empty half-width mobile stat cell.** The mobile rule assigns `flex: 1 1 46% !important` to every direct child of `[data-mobile-stat-strip]` that lacks `data-mobile-divider`. `StatDivider` lacks that attribute, so all three dividers match the cell rule rather than the hide rule and each consumes nearly half a row. Mark those elements as mobile dividers (or narrow the selector) so the named Scaling Laws offender produces actual two-column stats without large blank cells.
3. **`SPADESynthesis.jsx:420-438,507-519` still has no reliable touch painting path for the signature mobile interaction.** The semantic map sets `touchAction: 'none'` but listens only to `onMouseDown`, `onMouseMove`, window `mouseup`, and `contextmenu`; it has no pointer or touch down/move/up/cancel handling. `touch-action` only controls viewport manipulation and does not itself deliver the paint gesture, while compatibility mouse-event mapping for non-mouse pointers is optional in the W3C specification (`https://www.w3.org/TR/pointerevents/#compatibility-mapping-with-mouse-events`). Implement the brush with Pointer Events (including release/cancel and preferably pointer capture), or an equivalent explicit touch path, and verify that a finger can paint a continuous stroke at 375px.

### ADVISORY

- `FailureModes.jsx:143-152` and `MemoryArchitecture.jsx:266-330` retain fixed expanded `maxHeight` values (`420px` and `220px`). Narrow wrapping substantially increases the required height, so the hidden overflow may clip traces, mitigations, or memory steps on a phone. The browser backend was unavailable, so treat this as a directed 375px regression check and remove/override the cap if clipping reproduces.
- The scenario/task rows in `MultiAgent`, `ReActLoop`, and `ToolUseFlow`, plus Latent Space Explorer's active interpolation control row, still omit `flexWrap`. They remain contained because `WidgetCard` now scrolls every body horizontally, but wrapping these small controls would be calmer than forcing a nested swipe just to reach a tab or slider.
- `HarnessTaxonomy` deliberately keeps a 540px canvas and relies on the generic `WidgetCard` overflow container. If fixed-width scrolling is the intended solution, use the repository's explicit `ScrollableFigure` affordance so the overflow is local, deliberate, and consistent with the Q3 specification.

## Builder resolution (2026-07-15)

Re-verified required critique round 1 in full.

1. `RLHFPipeline`, SPADE's mechanism diagram, and `MultiAgent` now use explicit local horizontal scrollers at the phone breakpoint; their 580/560/616-unit SVGs retain a readable minimum width instead of scaling down.
2. `ScalingLaws` now marks `StatDivider` with `data-mobile-divider`, so the mobile two-column stat rule applies only to actual stat cells.
3. SPADE painting now uses Pointer Events with pointer capture and pointer-up/cancel cleanup. Coordinates continue to derive from the rendered SVG bounds, so touch strokes map to the semantic grid.
4. `LangChainComposition` now stacks its palette and composition view at the phone breakpoint, wraps its controls, and uses an explicit local scroller for its 390-unit flow diagrams.

`npm run check` passes after these fixes.

## Builder resolution (2026-07-15)

Regression check: re-verified all required findings from critique round 1. It is Q3's first critique round, so there are no earlier critique-required findings to carry forward. The original Q3 mobile pass also remains intact: opted-in panel rows stack at 375px, MDP Explorer converts scaled canvas coordinates before cell edits, the SPADE palette uses four mobile columns, and existing stat-strip dividers remain explicitly marked.

1. `RLHFPipeline`, SPADE's mechanism diagram, and `MultiAgent` keep their 580/560/616-unit SVG coordinate systems inside explicit local horizontal scrollers at the phone breakpoint, preserving readable label sizes.
2. `ScalingLaws` marks `StatDivider` with `data-mobile-divider`, so only stat cells receive the two-column mobile flex rule.
3. `SPADESynthesis` paints through captured Pointer Events. It converts client coordinates into the SVG viewBox, tracks the active pointer through up, cancel, and lost-capture cleanup, and interpolates between pointer cells so fast touch drags paint continuous strokes.

`npm run check` passes. The local preview server started, but no browser backend was available for the requested rendered desktop/375px and console inspection.

## Critique round 2 (2026-07-15)

Independent re-review: read `AGENTS.md`, the full queue and Q3 specification, the Phase Q section and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete append-only Q3 critique and git history, the cumulative Q3 diff, every current source artifact changed by Q3, and the explicitly named `ScalingLawCurves` artifact. Re-derived the narrow layout from the current nested page wrappers and mobile tokens, then re-verified every round 1 REQUIRED finding in the current source: the RLHF, SPADE, and Multi-Agent dense SVGs retain readable fixed widths in local scrollers; Scaling Laws dividers are excluded from mobile stat cells; and SPADE now paints continuous captured Pointer Events with up, cancel, and lost-capture cleanup. `npm run check` passes with the repository's existing unresolved font and KaTeX asset warnings. The local Vite server starts, but the browser runtime reports no available browser backend after the prescribed availability check, so this round does not claim rendered desktop/375px, touch, equation, or console observation. Q3 changes no textbook factual claims or citations, so no subject-matter primary-source spot-check applies.

### REQUIRED

1. **`src/components/diagrams/ch10/ScalingLawCurves.jsx:55-61,89-123,181-254` remains the explicitly named Q3 offender, but it still scales the complete 640-unit diagram to the phone column instead of scrolling or reflowing.** At 375px, `Layout.jsx:43-46,257-259` and `Transformers.jsx:58-63` both apply the 16px mobile chapter padding, leaving about 311px for the figure. The `width="100%"` SVG therefore renders at roughly 0.49 scale, turning its 9.5-13px title, ticks, point labels, legend, and bottom annotation into approximately 4.6-6.3px text. Q3 names `ScalingLawCurves` specifically and requires a stack or fixed readable scroller. Preserve a readable minimum width in `ScrollableFigure`/an equivalent local scroller, or provide a mobile layout; changing the neighboring `ScalingLaws` widget does not satisfy this named artifact.
2. **`src/components/widgets/ch24/MemoryArchitecture.jsx:102-110,144-146,209-243,477-509` still compresses its 540-unit four-panel memory diagram into unreadable mobile text.** After the nested chapter padding and the widget body's 12px side padding, the diagram has about 285px at a 375px viewport. Its 9-13px SVG labels render at roughly 4.8-6.9px. The new `data-mobile-stack` only moves the 180px stats panel below the left column; it does not reflow or preserve the width of the diagram inside that column. This is the same material failure mode Q3 fixed for RLHF, SPADE, and Multi-Agent, and it remains within the specified RL/agents multi-panel scope. Give `MemoryDiagram` a mobile-specific stacked representation or a local fixed-width horizontal scroller.
3. **`src/components/widgets/ch21/BenchmarkLeaderboard.jsx:43-46,65,145-167,456-470,519-583` still clips core chart annotations and many selected-point tooltips at the named 375px target.** The canvas is about 285px wide in the current nested layout. The ImageNet and HumanEval caveats are long 9px mono strings right-aligned at `W - 23` with no measurement, wrapping, or responsive legend, so their left portions fall outside the canvas; these caveats are precisely what prevent readers from treating a top-5 human number as a top-1 threshold or inventing a HumanEval human baseline. Tooltip placement also flips a 200px card wholly to one side without clamping: for an ImageNet 2014 point at approximately x=89px, the current formula produces `left ≈ -125px`. Put the caveats in a wrapped mobile legend (or otherwise keep them in bounds) and clamp/center narrow-canvas tooltips so this named offender is usable at 375px.

### ADVISORY

- `FailureModes.jsx:143-152` and `MemoryArchitecture.jsx:263-330` still cap expanded content at 420px and 220px while narrow wrapping increases content height. This carries round 1's directed clipping check forward; without a browser backend I cannot claim the rendered overflow, so it remains non-blocking.
- `Layout.jsx:43-46,257-259` and every chapter article both apply `--chapter-padding`, consuming 64px horizontally at 375px before widget padding. Removing one layer would give all mobile figures more room, but it is a broader layout cleanup rather than a substitute for explicit handling of dense diagrams.
- `HarnessTaxonomy` still relies on the generic `WidgetCard` body overflow for its 540px canvas. An explicit local `ScrollableFigure`-style affordance would make that horizontal interaction clearer, but the fixed canvas remains reachable and this does not block Q3.

## Builder resolution (2026-07-15)

Regression check: re-verified every REQUIRED finding from critique rounds 1 and 2, including the earlier resolved changes to RLHF, SPADE, Multi-Agent, Scaling Laws, and SPADE Pointer Events. The first round's fixed-width 580/560/616-unit diagram scrollers remain local and readable; Scaling Laws stat dividers remain excluded from the two-column mobile rule; and SPADE still captures, maps, interpolates, and cleans up Pointer Events through up, cancel, and lost capture.

1. `ScalingLawCurves` now uses the shared `ScrollableFigure` with a 640px minimum diagram width. At the Q3 breakpoint its complete labeled diagram scrolls locally instead of scaling its labels below a readable size.
2. `MemoryArchitecture` now gives `MemoryDiagram` a 540px minimum width inside an explicit, labeled local horizontal scroller. The four-panel diagram keeps its intended label sizes while the existing stats panel still stacks below it on a phone.
3. `BenchmarkLeaderboard` now renders only short in-canvas references and moves the ImageNet metric-mismatch and HumanEval-no-baseline caveats into a wrapped HTML note beneath the chart. The ImageNet top-5/non-top-1 and HumanEval caveats were rechecked against Russakovsky et al.'s ImageNet challenge paper and Chen et al.'s HumanEval paper. Tooltip dimensions and positions are now clamped to the rendered canvas with an 8px gutter, so selected points cannot place the card beyond either horizontal edge on a narrow canvas.

`npm run check` passes. The Vite preview server also served the local app, but this environment has no available browser backend for rendered desktop/375px, touch, equation, or console inspection.

## Critique round 3 (2026-07-15)

Independent re-review: read `AGENTS.md`, the full queue and Q3 specification, the Phase Q section and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete append-only Q3 critique and git history, the cumulative Q3 diff, and every current source artifact changed by Q3. Re-derived the 375px layout and re-verified all REQUIRED findings from critique rounds 1 and 2 in current source: the RLHF, SPADE, Multi-Agent, Scaling Law, and Memory Architecture diagrams now preserve readable fixed widths in local scrollers; Scaling Laws dividers remain excluded from mobile stat cells; SPADE still uses captured, interpolated Pointer Events with complete cleanup; and Benchmark caveats are outside the narrow canvas. `npm run check` passes with the repository's existing unresolved font and KaTeX asset warnings. A focused ESLint run on the two artifacts named below fails with the confirmed `H is not defined` error. The local Vite server serves `/ch/21` and `/ch/25`, but the in-app browser inventory is empty after the prescribed recovery check, so this round does not claim rendered desktop/375px, touch, equation, or console observation. Rechecked the current Benchmark caveats against Russakovsky et al.'s primary ImageNet paper (the 5.1% human result is top-5 error on 1,500 images) and Chen et al.'s primary HumanEval paper (it reports model pass rates and no human baseline); those caveats remain sound.

### REQUIRED

1. **`src/components/widgets/ch21/BenchmarkLeaderboard.jsx:475-487` crashes as soon as a reader hovers or selects any plotted model.** The round-2 tooltip repair destructures `{ toX, toY, W }` from `getCoords(...)` but then evaluates `H` on line 486. Once `ttModel` is non-null and the canvas ref exists, that render throws `ReferenceError: H is not defined`; the widget's error boundary replaces the complete interactive instead of showing the clamped tooltip. The production build does not lint and therefore passes, while focused ESLint confirms `486:52 error 'H' is not defined`. Include `H` from the measured coordinates (or derive the vertical bound another sound way), then exercise both mouse hover and the accessible model selector at desktop and 375px so the repaired clamp is proven on both paths.
2. **`src/components/widgets/ch25/LangChainComposition.jsx:647-654` clips the fixed-width mobile flowcharts instead of making them scrollable.** The wrapper opts into `[data-mobile-scroll]`, but its inline `overflow: 'hidden'` shorthand wins over `src/index.css:171-174` and keeps `overflow-x` hidden. At 375px the right column is roughly 285px wide while all three SVGs retain a 390px minimum width, so the rightmost 105px is unreachable rather than locally scrollable. This contradicts the builder's round-1 resolution note and leaves an agents-chapter multi-panel offender outside Q3's required stack-or-scroll treatment. Remove the conflicting shorthand or set an explicit effective horizontal scroller while retaining vertical clipping, then verify all three presets at 375px.

### ADVISORY

- `FailureModes.jsx:143-152` and `MemoryArchitecture.jsx:263-330` still cap expanded content at 420px and 220px while narrow wrapping increases content height. This remains a directed 375px clipping check because the unavailable browser prevents a rendered determination.
- `Layout.jsx:43-46,257-259` and each chapter article both apply `--chapter-padding`, consuming 64px horizontally at 375px before widget padding. Removing one layer would give every mobile widget more room, but it is broader than Q3's explicit offender fixes.
- `HarnessTaxonomy` still relies on the generic `WidgetCard` body overflow for its 540px canvas. An explicit local `ScrollableFigure`-style affordance would make the horizontal interaction clearer, but the fixed canvas remains reachable and this does not block Q3.

## Builder resolution (2026-07-15)

Regression check: re-verified every REQUIRED finding from critique rounds 1, 2, and 3. The earlier fixed-width scrollers for RLHF, SPADE, Multi-Agent, Scaling Law Curves, and Memory Architecture remain local and preserve their SVG label sizes; Scaling Laws stat dividers remain excluded from the two-column mobile rule; SPADE retains captured, interpolated Pointer Events with complete cleanup; and Benchmark caveats remain in the wrapped HTML note rather than the narrow canvas.

1. `src/components/widgets/ch21/BenchmarkLeaderboard.jsx` now takes `H` from `getCoords` together with `W`, `toX`, and `toY` before calculating the tooltip's vertically clamped position. The hover and accessible-selector paths both reach the same `ttModel` rendering branch, so the prior undefined identifier no longer throws when a model is selected.
2. `src/components/widgets/ch25/LangChainComposition.jsx` now gives the flowchart wrapper explicit `overflowX: 'auto'` and `overflowY: 'hidden'`. This no longer overwrites the `[data-mobile-scroll]` rule with an `overflow: 'hidden'` shorthand, so all three fixed-width preset SVGs remain horizontally reachable at 375px.

No advisories were taken because they are broader non-blocking layout follow-ups. `npm run check` passes. A local Vite server served the preview, but this environment has no browser backend after the prescribed recovery check, so rendered desktop/375px interaction, equation, and console observation could not be completed here.

## Critique round 4 (2026-07-15)

Independent re-review: read `AGENTS.md`, the full queue and Q3 specification, the Phase Q section and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete append-only Q3 critique and git history, the cumulative Q3 diff, every current source artifact changed by Q3, and the remaining RL/agent widget layouts implicated by the specification. Re-derived the 375px content width from the current nested page and widget padding, ran `npm run check` (passes with the repository's existing font and KaTeX asset warnings), and ran focused ESLint over every Q3-changed source artifact (zero errors; pre-existing warnings remain). Re-verified all REQUIRED findings from rounds 1–3 in current source: the RLHF, SPADE, Multi-Agent, Scaling Law Curves, and Memory Architecture fixed-width scrollers remain effective; Scaling Laws dividers remain excluded from mobile stat cells; SPADE retains captured, interpolated Pointer Events with complete cleanup; Benchmark caveats remain outside the narrow canvas and its tooltip now includes both measured dimensions; and LangChain's three fixed-width flowcharts have effective horizontal scrolling. The local Vite server starts, but the browser runtime exposes no browser backend after its prescribed availability check, so this round does not claim rendered desktop/375px, touch, equation, or console observation. Q3 adds no new textbook factual claims; I independently rechecked the retained Benchmark caveats against Russakovsky et al.'s primary ImageNet paper (the 5.1% human result is top-5 error on 1,500 images) and Chen et al.'s primary HumanEval paper (model pass rates are reported, but no human pass@1 baseline).

### REQUIRED

1. **`src/components/widgets/ch25/CrewAIvsAutoGPT.jsx:99-205,223-338,721-748` remains an unhandled agents-chapter multi-panel offender at 375px.** The widget keeps CrewAI and AutoGPT in a non-wrapping two-column row. With roughly 285px inside the mobile widget body, each column receives about 136.5px after the 12px gap, so its 280-unit SVGs render at approximately 0.49 scale. The 8.5–10.5px roster, goal-stack, task-node, and loop labels therefore become roughly 4.1–5.1px, well below a readable phone presentation. This is exactly the side-by-side agents layout Q3's stack-or-scroll requirement covers. Stack the two comparison columns at the phone breakpoint, or preserve each diagram at a readable fixed width in an explicit local scroller; also verify the shared goal and controls remain contained after the reflow.
2. **`src/components/widgets/ch25/ClaudeCodeArchitecture.jsx:331-526` and `src/components/widgets/ch25/LangGraphStateMachine.jsx:526-568` still shrink dense agent diagrams into unreadable phone graphics instead of preserving a readable width.** At the same roughly 285px widget-body width, the 582-unit Claude Code architecture renders at about 0.49 scale, turning its 8–12px connection, subsystem, bullet, and loop labels into about 3.9–5.9px text. The 700-unit LangGraph state machine renders at about 0.41 scale, turning its 9–11px edge and node labels into about 3.7–4.5px text. Both are among the Chapter 25 agent layouts the Q3 audit was meant to catch, and both use plain `width="100%"` SVGs without a mobile reflow or fixed-width scroller. Give each diagram an explicit readable mobile treatment and verify every selectable subsystem/preset remains reachable at 375px.

### ADVISORY

- `RLVRvsRLHF.jsx:297-346,477-545` keeps its comparison rows and worked example in two narrow columns separated by 49px of divider and margin. Unlike the SVG failures above, its HTML text remains at its declared size and wraps, so this is non-blocking; stacking the paired columns would reduce extreme wrapping and better match Q3's mobile comparison pattern.
- `FailureModes.jsx:143-152` and `MemoryArchitecture.jsx:263-330` still cap expanded content at 420px and 220px while narrow wrapping increases content height. This remains a directed rendered clipping check because the unavailable browser prevents an observed determination.
- `HarnessTaxonomy` still relies on generic `WidgetCard` overflow for its 540px canvas. An explicit local `ScrollableFigure`-style region would make the horizontal interaction clearer, but the fixed canvas remains reachable and this does not block Q3.

## Builder resolution (2026-07-15)

Regression check: re-verified every REQUIRED finding from critique rounds 1, 2, 3, and 4. The round-1 RLHF, SPADE, and Multi-Agent dense diagrams retain their local readable-width scrollers; Scaling Laws still excludes dividers from two-column mobile cells; and SPADE retains captured, interpolated Pointer Events with up, cancel, and lost-capture cleanup. The round-2 Scaling Law Curves and Memory Architecture scrollers remain effective, while Benchmark's metric caveats remain outside the narrow canvas with a measured, clamped tooltip. The round-3 Benchmark tooltip still reads both measured dimensions, and LangChain's fixed-width flowcharts retain effective local horizontal scrolling.

1. `src/components/widgets/ch25/CrewAIvsAutoGPT.jsx` now opts the shared-goal banner and the CrewAI/AutoGPT comparison row into the existing phone stack. Below the Q3 breakpoint each 280-unit diagram occupies the complete widget width instead of half of it, while the already wrapping controls remain contained.
2. `src/components/widgets/ch25/ClaudeCodeArchitecture.jsx` now places its 582-unit architecture SVG in a labeled `data-mobile-scroll` region and preserves a 582px minimum rendered width. Every subsystem remains reachable by the explicit local horizontal scroll rather than being scaled below readable label sizes.
3. `src/components/widgets/ch25/LangGraphStateMachine.jsx` now places its 700-unit state-machine SVG in a labeled `data-mobile-scroll` region and preserves a 700px minimum rendered width. The preset controls continue to wrap independently, and every state and transition remains reachable by local horizontal scrolling.

No educational claims or citations changed in this resolution, so no new source check was needed. Focused ESLint over the three changed artifacts reports no errors (only pre-existing warnings). `npm run check` passes. The local Vite preview responds for Chapter 25, but this environment's browser runtime has no backend, so rendered desktop/375px interaction, equation, and console observation could not be completed here.
