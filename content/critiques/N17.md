verdict: approve

## Critique round 1 (2026-07-15)

Independent review method: read `AGENTS.md`, the N17 queue row, the queue protocol, Appendix B, Appendix C's N17 specification, the V2 editorial standard, curriculum, both N17 build commits, and every current N17 chapter, diagram, and widget source line. No prior N17 critique history exists. Re-derived the displayed equations and widget calculations, ran `npm run check` (passes; Vite still reports the repository's existing unresolved font and KaTeX asset warnings), and checked the cited S4, Mamba, linear-attention, RWKV, Jamba, Griffin, and Mamba-2 claims against their primary papers or official project. A rendered browser pass could not be completed because no browser binding was available; the temporary local server was confirmed stopped.

### REQUIRED

1. **`StateSpaceModels.jsx:47-50` does not implement Appendix C's continuous-SSM-to-discretization teaching step.** The chapter says only that a continuous form exists, then presents already-discretized matrices `\bar A` and `\bar B`. It never writes the continuous dynamics, introduces a step size, or explains how discretization produces the recurrence and convolution kernel. This is a named core requirement of N17, not optional depth. Add the continuous equation and a concrete discretization (with a complete symbol gloss), then derive or explicitly connect the resulting discrete recurrence to the kernel used by `SSMDuality`.

2. **`ComplexityCurves.jsx:18-35` plots curves against an inconsistent sequence-length axis.** The equally spaced tick labels `1K, 4K, 16K, 64K, 256K` define a logarithmic x-axis, but the paths are generated as linear functions of normalized pixel position (`u`) and `u^2`. Those are not `O(T)` and `O(T^2)` curves at the labeled values, so the figure encodes a false relative shape. Compute the paths from the actual tick values under a declared linear, log-linear, or log-log scale, and label that scale honestly.

3. **`ComplexityRace.jsx:26,81` and `StateSpaceModels.jsx:67-68` materially understate SSM recurrent-state memory.** The widget hard-codes scan state as `2d`, and the chapter states scan state is proportional only to `L_scan d`. For the Mamba family discussed here, the recurrent state also has an SSM state dimension: the official implementation stores state over `d_inner * d_state` (with `d_state=16` for the shown Mamba default and typically 64 or 128 for Mamba-2). Omitting that dimension can undercount the fixed recurrent state by orders of magnitude and makes the memory comparison quantitatively misleading. Expose a state dimension `N`/`d_state` in the model and formulas, or explicitly recast the widget as a toy state of declared dimension rather than an SSM/Mamba memory estimate.

4. **`StateSpaceModels.jsx:63` directs the reader to a control value that `ComplexityRace.jsx:65` cannot reach.** The `tryThis` instruction says to move the length slider to 262,144 tokens, but the slider's maximum is 65,536. The V2 standard requires a directed interaction that can actually be performed. Make the range and accounting safe through 262,144 or change the instruction and predicted observation to a reachable value.

5. **`StateSpaceModels.jsx:70-71` directs readers to nonexistent `HybridLayerMixer` presets.** Both the hand-off and `tryThis` say to compare “attention-heavy” and “scan-heavy” presets, while `HybridLayerMixer.jsx` exposes only per-block type buttons and one “restore hybrid preset” button. The prescribed experiment therefore cannot be followed. Add the two named presets with truthful outputs, or rewrite the directive to specify the exact manual block edits that reproduce the comparison.

### ADVISORY

- `SelectiveScan.jsx` is responsibly labeled as a toy, but its direct retention/write gates are looser than Mamba's actual input-dependent `\Delta`, `B`, and `C` parameterization. Showing that mapping, even briefly, would make the abstraction more faithful without requiring a full Mamba implementation.
- The mid-2026 currency pass could acknowledge Mamba-3 (March 2026) as a brief frontier note. The official Mamba project now lists it alongside Mamba and Mamba-2; Appendix C does not require a full treatment.
- Inline markers `[1]` through `[7]` are not linked to their `ref-N` anchors, despite the V2 citation convention. This is inherited elsewhere in the book, but N17 should follow the declared convention when revised.

The chapter has a clear lede, four specified sections, four analytic interactions, actionable exercises, and mostly accurate primary-source summaries. Approval is blocked by the missing core derivation, false complexity-figure geometry, misleading SSM memory accounting, and two impossible directed interactions above.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED finding from critique round 1 against the current chapter, diagram, and widget sources. The continuous-to-discrete derivation, log-scale figure geometry, SSM state-dimension accounting, reachable sequence-length control, and named hybrid presets all hold together without undoing another round-1 fix.

1. `src/pages/ch17/StateSpaceModels.jsx` now writes the continuous SSM, defines a zero-order-hold token step `Δ`, gives `Ā = e^(ΔA)` and the corresponding `B̄` integral, and unrolls the discrete recurrence into `K_ℓ = CĀ^ℓB̄`. It explicitly identifies the scalar `Ā=a`, `B̄=C=1`, `D=0` specialization used by `SSMDuality`.
2. `src/components/diagrams/ch17/ComplexityCurves.jsx` now derives every plotted point from the actual 1K, 4K, 16K, 64K, and 256K sequence lengths. Both axes are declared logarithmic, with attention using normalized `T(T+1)/2` work and scans using normalized `T` work.
3. `src/components/widgets/ch17/ComplexityRace.jsx` now exposes the SSM state dimension `N` and accounts for its recurrent state as `d·N`; the chapter's hybrid memory expression now likewise states `L_scan·dN` and defines all three factors.
4. `ComplexityRace` now permits the chapter-directed endpoint of 262,144 tokens.
5. `src/components/widgets/ch17/HybridLayerMixer.jsx` now supplies real attention-heavy, hybrid, and scan-heavy presets, so the chapter's directed comparison is executable and updates the disclosed accounting readouts.

No advisory changes were taken; this resolution is limited to the five REQUIRED findings. `npm run check` passes (with the repository's existing unresolved font-asset warnings).

## Critique round 2 (2026-07-15)

Focused re-review method: rechecked only the five REQUIRED findings from critique round 1 against the current `StateSpaceModels.jsx`, `ComplexityCurves.jsx`, `ComplexityRace.jsx`, and `HybridLayerMixer.jsx` artifacts, and ran `npm run check` (passes, with the repository's existing unresolved font and KaTeX asset warnings). Findings 2 through 5 are resolved: the complexity diagram now uses declared log scales and actual sequence lengths, SSM recurrent memory includes `dN`, the length control reaches 262,144, and all three named hybrid presets exist and update the disclosed readouts.

### REQUIRED

1. **`StateSpaceModels.jsx:51` still gives an initial condition inconsistent with its recurrence and convolution sum, so round-1 finding 1 is not fully resolved.** The chapter writes `x_t = \bar A x_{t-1} + \bar B u_t`, then says that unrolling it "with `x_0=0`" gives a sum whose `\ell=0` term makes the current input contribute to `x_t`. Under the widget's zero-based convention, the first input produces `x_0`; it cannot also leave `x_0=0`. Use a pre-input state such as `x_{-1}=0`, or re-index tokens from `t=1` and adjust the convolution limits consistently. The continuous-to-discrete derivation is N17's named core teaching step, so this off-by-one contradiction remains blocking.

### ADVISORY

- The three advisories from critique round 1 were outside this focused re-review and remain advisory; none is escalated.

Round-1 REQUIRED findings 2 through 5 are verified resolved. Approval remains blocked only by the inconsistent initial-state indexing in the core recurrence-to-convolution derivation.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED finding from critique rounds 1 and 2 against the current chapter, diagram, and widget sources. Round-1 findings 2 through 5 remain resolved, and the continuous-to-discrete derivation now uses one consistent zero-based state convention from recurrence through convolution.

1. `src/pages/ch17/StateSpaceModels.jsx` now declares the pre-input state as `x_{-1}=0` in the discrete recurrence, states that the first zero-based token produces `x_0`, and unrolls that same recurrence into the causal convolution sum. Its scalar `SSMDuality` explanation and the matching exercise now use `x_{-1}=0` as well.

No advisory changes were taken. `npm run check` passes.

## Critique round 3 (2026-07-15)

Independent final review method: re-read the current chapter, diagram, and all four widget sources; re-derived the recurrence and convolution indexing; re-verified every REQUIRED finding from critique rounds 1 and 2; ran `npm run check` (passes, with the repository's existing unresolved font and KaTeX asset warnings); and checked the SSM state/work dimensions against the Mamba paper and official implementation. Per the user's direction, no preview was launched.

### REQUIRED

1. **`ComplexityRace.jsx:19-27,54-57,73-89` exposes the SSM state dimension `N` but omits it from the widget's claimed SSM work.** The widget correctly models recurrent state as `d·N`, yet computes and labels scan work as `3T·d`; moving `N` from 4 to 128 therefore leaves both the SSM work bar and the displayed attention/SSM work ratio unchanged. A selective SSM updates and reads a state over the `d × N` dimensions (the Mamba formulation and official implementation use state shaped over model channels and `d_state`), so its scan work depends on `N` even though it remains linear in `T`. Omitting that factor can understate the modeled scan work by up to the widget's full 32× `N` range and makes the quantitative race materially misleading. Include `N` in a clearly disclosed SSM work model and ratio, or remove the `N` control and recast all work bars as explicitly arbitrary asymptotic illustrations that do not claim comparable operation counts.

### ADVISORY

- The recurrence now uses `x_{-1}=0` consistently in the discrete equation, convolution unrolling, scalar worked example, and exercise. Round-1 findings 2 through 5 remain resolved: the diagram uses actual sequence lengths on declared log scales, SSM memory includes `dN`, the length control reaches 262,144, and the three named hybrid presets remain present.
- The three advisories from critique round 1 remain non-blocking and are not escalated.

The chapter's core derivation is now internally consistent and all previously requested fixes remain present. Approval is blocked only by the newly identified dimensional inconsistency in the analytic SSM work comparison.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED finding from critique rounds 1, 2, and 3 against the current chapter, diagram, and all four widget sources. The continuous-to-discrete derivation remains consistently zero-based, the complexity figure still derives its log-scale geometry from the labeled token values, the reachable length control and named hybrid presets remain present, and SSM state memory remains `d·N`.

1. `src/components/widgets/ch17/ComplexityRace.jsx` now models SSM prefill work as `3T·d·N`, where `3` is explicitly disclosed as a proportional accounting constant for each token's update and read across the `d × N` SSM state. The SSM bar label, explanatory prose, and displayed attention/SSM work ratio now all include `N`, so moving the state-dimension control changes the work comparison while the SSM term remains linear in `T`.

No advisory changes were taken. `npm run check` passes (with the repository's existing unresolved font-asset warnings).

## Critique round 4 (2026-07-15)

Focused final review method: read the full N17 critique history and the current `ComplexityRace.jsx`; re-derived its work and ratio calculations; re-verified every REQUIRED fix from critique rounds 1 through 3 against the current chapter, complexity diagram, and hybrid widget; and ran `npm run check` (passes, with the repository's existing unresolved font and KaTeX asset warnings). Per the user's direction, no preview was launched.

### ADVISORY

- The three advisories from critique round 1 remain non-blocking and are not escalated.

No REQUIRED issue remains. The disclosed SSM work model is now `3T·d·N`: it changes with both width `d` and state dimension `N`, remains linear in sequence length `T`, and feeds the bar and attention/SSM ratio consistently. The continuous-to-discrete derivation and zero-based indexing remain correct, the complexity diagram retains truthful log-scale geometry, SSM memory remains `d·N`, the length control still reaches 262,144, and all three named hybrid presets remain present and functional. N17 is materially truthful and approved.
