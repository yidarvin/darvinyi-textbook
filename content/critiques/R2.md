verdict: approve

## Critique round 1

Re-derived independently from the artifacts in commit `c0e43c5` (build: R2 -- ch09 attention widget fidelity). Re-read `CRITIQUE.md`'s M-01 entry and its Minor ch09-entropy-units line, `context/STYLE_GUIDE.md`'s widget-fidelity rule, both changed widgets in full, and the unchanged `QKVInspector.jsx` reference pipeline. Independently reimplemented both widgets' math in a standalone Node script (`matMul`/`transpose`/`softmax`/`softmaxRows` copied verbatim from the source) and numerically re-derived every claim below rather than trusting the commit message.

**AttentionHeatmap.jsx**
- `buildWeights` genuinely computes `S_raw = QK^T`, scales by `√4`, and row-softmaxes (`AttentionHeatmap.jsx:55-61`) — not a disguised lookup. Traced by hand and by script for sentence A (`The cat sat on the mat`): all six rows sum to 1.0000, and the commit's specific claim reproduces exactly — row "The" gives `self=0.253` vs `→"the"=0.261`, row "cat" gives `self=0.248` vs `→"mat"=0.267`, i.e. same-type affinity genuinely exceeds self-attention, not asserted.
- `TYPE_VEC`/`OFFSET_Q`/`OFFSET_K` are hand-authored per-token feature inputs (analogous to `QKVInspector`'s hard-coded `Q_MAT`/`K_MAT`), but the displayed weights are the real output of the matmul+softmax pipeline applied to them, matching the established house pattern rather than a fabricated output table.
- Entropy label reads "Entropy H (nats)" (`:459`) and uses natural log (`calcEntropy`, `:111-113`). Zero hits for "bits"/"log2" in the file.

**MultiHeadAttention.jsx**
- All four heads (`headLocal`, `headGlobal`, `headIdentity`, `headSemantic`, `:65-106`) run through the shared `attention()` helper (`matMul` → scale by `√4` → `softmaxRows`), not hard-coded matrices. Verified numerically for sentence A (5 tokens): every row of every head sums to 1.0000.
- Local: algebraically expanded `Q_i·K_j` by hand and confirmed by script it equals exactly `-(i-j)^2 · TEMP` for arbitrary (i,j) pairs (e.g. i=4,j=1 → -4.5000 both computed and by formula) — a genuine closed-form quadratic-distance kernel, not a labeled approximation.
- Global: recomputed the full weight matrix — every query row's argmax is position 0 regardless of query identity (0.482–0.486 to token 0 across all 5 rows), matching the "query-agnostic broadcast" claim.
- Identity: `Q=K` on a unit circle scaled by 2.5; recomputed diagonal weight is 0.808 for every row (off-diagonal ≤0.093), a genuinely sharp near-one-hot pattern, not merely labeled sharp.
- Semantic: recomputed cross-type argmax for every row of sentence A — ART/VERB/ADJ rows peak on the NOUN positions (0.336–0.435) and NOUN rows peak on the VERB position (0.435), i.e. real cross-type peaks (verb/modifier→noun, noun→verb) rather than raw self-similarity. No row's argmax is a same-type token.
- Entropy: recomputed all four heads' avg entropy for sentence A — Local 1.363, Global 1.408, Identity 0.648, Semantic 1.462 — matching the commit message's claimed 1.36/1.41/0.65/1.46 nats exactly (own independent reimplementation, not the widget's own output). `calcEntropy` uses `Math.log` (`:137-142`), and both rendered labels read "(nats)" (`:432`, `:480`). Zero hits for "bits"/"log2" in the file.
- Prose cross-check: `src/pages/ch09/Attention.jsx:338-350` says "Head 3 stays nearly diagonal... the lowest of the four... while Head 4 runs noticeably higher." Recomputed entropies confirm: Identity (Head 3) = 0.648 is the minimum of the four, and Semantic (Head 4) = 1.462 is the maximum — the prose is accurate against the new real computation.

**Scope.** `git show c0e43c5 --stat` touches exactly `prompts/queue.md`, `src/components/widgets/ch09/AttentionHeatmap.jsx`, and `src/components/widgets/ch09/MultiHeadAttention.jsx` — no other chapter or file is in this commit.

**Gate.** `npm run check` run fresh: `vite build` succeeds, Playwright smoke passes 28/28 with no console-error failures.

**Prose accuracy (ch09 Attention.jsx).** Read the surrounding prose around both widget mounts (`:149-163`, `:320-350`). The `AttentionHeatmap` lede ("single softmax lookup... regardless of distance") and its `tryThis` remain accurate now that the softmax is real. The `MultiHeadAttention` lede and `tryThis` (entropy-ordering claim) checked against the recomputed numbers above and hold.

No REQUIRED findings.

**ADVISORY (non-blocking).**
- The in-code comments explaining that Q/K vectors are hand-authored toy inputs feeding a real computation (e.g. `MultiHeadAttention.jsx:50-58`) are not visible to a reader — only to someone reading source. This matches the already-accepted `QKVInspector.jsx` precedent (also not disclosed in-widget) and satisfies STYLE_GUIDE's "either implement the real computation or disclose" rule via the first arm, so it is not a blocker; a future pass could consider a one-line "toy Q/K, real softmax" caption for extra reader trust, consistent with `ResidualStream.jsx`'s style, but this was not required by M-01's fix criteria (R-05's Accept line only requires computed-from-inputs OR disclosure, and computed-from-inputs is satisfied here).
- `HEAD_DESCS` text (unchanged by this commit, pre-existing and out of R2's scope) makes claims like "This pattern is observed in BERT-style models" with a code-comment-only citation (Voita et al. 2019, Clark et al. 2019) that isn't surfaced as an inline `[N]` citation. Not part of M-01 or the entropy-units Minor finding this queue item targets; flagging only for a possible future citation-completeness pass, not for R2.
