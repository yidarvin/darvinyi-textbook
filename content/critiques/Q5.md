verdict: resolved

## Critique round 1 (2026-07-15)

Independent review: read `AGENTS.md`, the complete queue and Q5 specification, the Phase Q section and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete Q5 build commit (`f5a93db`), the retired pre-V2 `audit-report.md` and `mobile-pass-report.md`, and every current artifact changed by Q5. No prior Q5 critique history exists. Re-ran the global source inventories from the current artifacts and traced chapter metadata into the home-page cards. `npm run check` passes: the build completes and all 27 Playwright route cases pass, with the existing unresolved font-asset warnings. Independently re-ran Lighthouse 12.8.2 against the local production home route in mobile mode; the recorded category scores reproduce exactly at 98 Performance, 100 Accessibility, 100 Best Practices, and 100 SEO. The repeat measured FCP/LCP/Speed Index/TTI at 1.9 s and render-blocking opportunity at 450 ms rather than the recorded 2.0 s and 600 ms, normal local lab-run variation. Q5 changes no textbook factual claims or citations, so no subject-matter primary-source check applies.

### REQUIRED

1. **`src/data/chapters.js:16,26` and `src/pages/Home.jsx:118-120` — the home page publishes stale widget counts that the final consistency audit did not compare with the chapter artifacts.** The 25 chapter pages contain 123 widget invocations with 123 `tryThis` props, but `CHAPTERS.reduce((n, c) => n + c.widgets, 0)` is 125: Chapter 5 advertises six widgets while `TrainingTechniques.jsx` renders five, and Chapter 9 advertises four while `Attention.jsx` renders three. Because `Home.jsx` renders this metadata directly, readers see two false counts. Correct the two metadata values and make the Q5 audit explicitly compare per-chapter metadata with the corresponding widget invocations, not only the aggregate active-widget inventory.

2. **`context/STYLE_GUIDE.md:60` and all 25 chapter pages — inline citation markers remain unlinked despite the book-wide V2 convention, and Q5's citation check misses that requirement.** The style guide requires plain `[N]` markers "anchor-linked to the References section." `Citations.jsx:65-88` creates the `ref-N` targets, but there is no inline `href="#ref-N"` path anywhere in `src/pages/`; representative prose such as `Attention.jsx:352-361` still emits bare text `[5]` and `[6]`. Counting only double-bracket markers therefore lets an explicit V2 convention remain globally unmet. Implement one consistent linked inline-marker path across the chapters and record a check that proves every marker resolves to an existing footer target.

3. **`audit-report.md:23-30,53-59` — the replacement audit contains incorrect evidence and a reproduction command that cannot run as written.** The current source has 86 `input[type="range"]` controls, not 85. The report also says the V2 style guide bans em dashes and stock phrasing, but `context/STYLE_GUIDE.md` contains neither rule; its 1,392-line dash count comes from all of `src/pages/` (including `Home.jsx` and `NotFound.jsx`), not the chapter prose named in the table. Finally, the fenced shell command ends three lines with `\\`: Bash treats each pair as a literal backslash and terminates the command instead of continuing it. Re-derive and correct the recorded checks against the actual repo rubric, and use single trailing backslashes (or one command line) for the Lighthouse reproduction command.

4. **`context/PROJECT_OVERVIEW.md:93-103` — the refreshed architecture document still describes a generated route table that does not exist.** The overview says `App.jsx` generates its route table from `src/data/chapters.js`, while current `App.jsx:13-37,116-140` manually declares all 25 lazy imports and all 25 `<Route>` elements. Q5 explicitly requires a final pass over `context/` documents, so the refreshed overview must describe the current routing architecture rather than a proposed one.

### ADVISORY

- `context/CURRICULUM.md:1` and `context/PROJECT_OVERVIEW.md:75` still call the completed curriculum a V2 "target." Changing those headings to "current" or simply "V2" would finish the status refresh, but the body text is already clear.
- The Lighthouse category baseline is reproducible. Consider retaining the exact JSON command/output location or documenting expected lab-run variance so later readers compare category scores and broad diagnostics rather than treating tenths of a second as a regression.

## Builder resolution (2026-07-15)

Regression gate: re-verified all REQUIRED findings from critique round 1 against the current metadata, rendered chapters, audit record, and routing source; all four now hold. There are no earlier Q5 rounds to re-verify.

1. Corrected Chapter 5 and Chapter 9 widget metadata in `src/data/chapters.js` from 6→5 and 4→3. `tests/routes.spec.js` now compares every chapter's published total to its rendered `.widget-card` count, so the audit enforces the 25 per-chapter comparisons rather than only the 123-widget aggregate.
2. Added `src/components/shared/CitationLinkifier.jsx` at the lazy chapter-route boundary. It turns each prose `[N]` marker with a local `ref-N` target into an accessible anchor, preserves existing chapter markup, and excludes equations, widgets, existing links, and the reference list. `Citations.jsx` marks that list explicitly, and the route smoke test rejects both bare resolvable markers and broken citation targets across all 25 chapters.
3. Re-derived the audit evidence in `audit-report.md`: the source range-control count is 86; the unsupported em-dash and stock-phrase findings are removed; the per-chapter metadata and citation-target checks are recorded; and the Lighthouse command uses valid single-backslash continuations. The report also documents expected local-lab timing variance.
4. Corrected `context/PROJECT_OVERVIEW.md` to describe the actual explicit lazy imports and `<Route>` declarations in `App.jsx`, while retaining `chapters.js` as the navigation/home metadata source.

Took both advisories: renamed the completed curriculum headings from V2 "target" to V2, and documented Lighthouse's repeat-run diagnostic variance. `npm run check` passes (production build and all 27 Playwright route checks).

## Critique round 2 (2026-07-15)

Independent re-review: read `AGENTS.md`, the complete queue and Q5 specification, the Phase Q section and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete append-only Q5 critique and git history, both Q5 implementation commits, the retired pre-V2 audit documents, and every current Q5 artifact. Re-verified all four round 1 REQUIRED findings from the current metadata, routes, rendered chapters, tests, audit report, and context overview; all four fixes hold. `npm run check` passes outside the restricted sandbox, including all 27 Playwright route cases; the initial sandboxed attempt could not bind the preview port and was not a repository failure. A separate production-browser inventory reproduced 123 rendered widgets and 97 live static diagrams, and checked all 372 rendered citation links across the 25 chapters with zero bare, orphaned, or broken markers. Lighthouse 12.8.2 independently reproduces the recorded mobile category scores at 98 Performance, 100 Accessibility, 100 Best Practices, and 100 SEO, with the documented repeat-run values of 1.9 s for FCP/LCP/Speed Index/TTI and 450 ms render-blocking opportunity. Q5 changes no textbook subject-matter claims or citations, so no primary-source factual spot-check applies.

### REQUIRED

1. **`src/index.css:1-13`, the production bundle, and `context/PROJECT_OVERVIEW.md:120` — the final audit treats unresolved font warnings as benign even though none of the self-hosted text or KaTeX fonts load in production.** `npm run check` emits unresolved-path warnings for every `@fontsource` and KaTeX face; `dist/` contains no `.woff`, `.woff2`, or `.ttf` files while its CSS still points to `assets/files/...` and `assets/fonts/...`. In a local production preview those requests receive the SPA's `text/html` fallback, and after `document.fonts.ready`, `document.fonts.check(...)` is false for Inter 400, JetBrains Mono 400, Crimson Pro 600, and KaTeX_Main 400. Readers therefore get fallback typography and math fonts despite the overview's statement that KaTeX and the other web fonts are self-hosted. Make Vite emit and serve the required font assets, add a production check that fails on missing/non-font font responses, and update the Q5 audit evidence to record the verified result.

2. **`context/DESIGN_SYSTEM.md:32-33,157-191,254-263` and `context/WIDGET_SPEC.md:3-9,59-69,96-357` — the required final pass over `context/` left two unmarked pre-V2 specifications that contradict the completed textbook.** The design system still instructs maintainers to load Google Fonts, limits prose to 2–4 sentences and then bans more than 2–3 sentences between widgets, and publishes the old six-part navigation taxonomy. The widget spec says no instruction is needed beyond control labels, endorses hover-only inspection without a keyboard/tap equivalent, numbers Statistical Learning/Neural Networks/Optimization as Chapters 1–3, and presents only those pre-migration widget inventories as the chapter specification. Those instructions conflict with the V2 fuller-textbook, directed-widget, accessibility, self-hosting, and 25-chapter conventions in `STYLE_GUIDE.md`. Refresh them to current V2 guidance, or explicitly retire and label them as historical artifacts with pointers to the current sources of truth; reflect that disposition in `audit-report.md`.

### ADVISORY

- Extend the route smoke's network audit beyond console errors. The missing font URLs return status 200 with `text/html`, so neither the current `>=400` intuition nor Lighthouse's category scores expose them; asserting successful `document.fonts.check(...)` calls or font MIME types would make the regression gate discriminating.
- The 98 diagram source files include one deliberately parked capsule diagram, while the rendered live total is 97. Calling the audit row “Live static diagrams” would make the scope explicit and keep future source-inventory reproductions from appearing to disagree.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED fix from critique rounds 1 and 2. Round 1's Chapter 5/9 metadata, linked citation markers, corrected audit evidence, and explicit routing description all still hold. For round 2, the production build now emits font assets and the two pre-V2 context documents are explicitly retired with current-source pointers.

1. Moved the `@fontsource` and KaTeX CSS imports from `src/index.css` to module imports in `src/main.jsx`. Vite now fingerprints and emits the required font assets in `dist/assets/`; the imports use the shipped Latin text-font subsets plus the KaTeX font set.
2. Extended the home-route production smoke in `tests/routes.spec.js`. It explicitly loads Inter 400, JetBrains Mono 400, Crimson Pro 600, and KaTeX_Main 400 before asserting `document.fonts.check(...)`, so a missing asset or an HTML fallback response fails the check.
3. Updated `audit-report.md` with the production-font result, the font regression method, and the advisory's explicit “Live static diagrams” scope (97 rendered diagrams; one parked capsule source diagram).
4. Retired `context/DESIGN_SYSTEM.md` and `context/WIDGET_SPEC.md` as unambiguous pre-V2 historical references and pointed maintainers to `STYLE_GUIDE.md`, `CURRICULUM.md`, `V2_PLAN.md`, runtime CSS, and widget source. Updated `context/PROJECT_OVERVIEW.md` to label their disposition and accurately describe the Vite-managed font imports.

Took both round 2 advisories because they directly strengthen the final audit. `npm run check` passes: the production build emits 77 font files and all 27 Playwright production-route checks pass.

## Critique round 3 (2026-07-15)

Independent re-review: read `AGENTS.md`, the complete queue and Q5 specification, the relevant Phase Q, Editorial Standard, convention, verification, and Appendix B sections of `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete append-only Q5 critique and git history, all three Q5 implementation commits, every current Q5 artifact, every tracked document and HTML artifact under `context/`, and the text of the tracked OpenCode guide. Re-verified every REQUIRED fix from rounds 1 and 2: the 25 per-chapter widget totals agree with the 123 rendered widgets; a production-browser inventory finds 372 citation links across 359 targets with no bare, orphaned, or broken markers; the reproduction command and explicit route description remain correct; the build emits 77 font files; and direct browser loads return one real face and a `200 font/woff2` response for each required Inter, JetBrains Mono, Crimson Pro, and KaTeX_Main descriptor. `npm run check` initially could not bind the sandboxed preview port, then passes outside that restriction with all 27 production-route cases. A separate browser sweep of all 25 chapters finds the KaTeX failures below. `npm run lint` exits 1 with 42 errors and 204 warnings. Two fresh Lighthouse 12.8.2 mobile runs against the current production preview reproduce one another at 94 Performance, 100 Accessibility, 100 Best Practices, and 100 SEO. Q5 changes no textbook claims or citation metadata, so no subject-matter primary-source spot-check applies.

### REQUIRED

1. **`src/pages/ch07/WordEmbeddings.jsx:501-504`, `src/pages/ch22/EvaluatingLLMsAndAgents.jsx:40-71`, and `tests/routes.spec.js:16-105` — the final global audit misses reader-visible broken equations because the route smoke never checks KaTeX's silent error output.** A production-browser sweep finds four `.katex-error` nodes: Chapter 7's contextual-embedding display passes raw underscores inside `\text{...}`, and Chapter 22's perplexity display, pass@k display, and worked binomial expression contain JavaScript strings with single LaTeX backslashes that become control characters such as form feed and backspace. The remaining Chapter 22 judge and agent-score expressions do not produce `.katex-error`, but still lose commands such as `\operatorname`, `\sigma`, `\left`, and `\mathrm`, visibly rendering command names and malformed labels. `MathBlock` deliberately uses `throwOnError: false`, so all 27 route cases and the console-error assertion stay green. Correct every malformed Chapter 7/22 expression, verify the intended rendered notation, and make the production route pass assert zero `.katex-error` nodes on every chapter so this Appendix B correctness check can fail when an equation silently degrades.

2. **`audit-report.md:32-47` — the recorded Lighthouse baseline predates the current font pipeline and no longer describes the audited build.** The report's capture time is `2026-07-16T02:33:05.114Z`, before the latest font-resolution commit, and records 98 Performance, 2.0 s FCP/LCP/Speed Index/TTI, CLS 0, and 600 ms render-blocking opportunity. Two consecutive runs against the current `f577739` production build at `03:13Z` and `03:14Z` both measure 94 Performance, 2.3 s FCP/LCP/Speed Index/TTI, CLS 0.076, and 900 ms render-blocking opportunity; Accessibility, Best Practices, and SEO remain 100. This is stable post-change evidence, not the fractional variance the report documents. Re-capture and record the baseline from the final build after the remaining fixes (whether or not the performance difference is optimized) so Q5's named Lighthouse deliverable is current.

3. **`context/CURRICULUM_VISUAL.html`, `context/MOCKUP_SHELL.html`, `context/REFERENCE_WIDGET.html`, and `context/darvinyi_textbook_OpenCode_Guide.docx` — the final context-document pass still leaves four unmarked V1 artifacts that contradict the completed textbook.** `CURRICULUM_VISUAL.html` presents the old six-part curriculum, V1/new-build badges, old chapter ownership, hover instructions, and a remote Google Fonts import. `MOCKUP_SHELL.html` is an old Chapter 2 mockup with hover-only teaching and a visible widget placeholder. `REFERENCE_WIDGET.html` imports Google Fonts remotely and uses unlabeled ranges plus non-semantic div controls, while `PROJECT_OVERVIEW.md` still calls it the built reference implementation. The Word guide says the project has 17 chapters, instructs readers to install the removed `d3` and `recharts` dependencies, restores remote font imports, and bootstraps the already-built app from scratch. Refresh the artifacts if they still serve a current purpose, or delete/explicitly retire them as historical with current-source pointers; then make `PROJECT_OVERVIEW.md` and `audit-report.md` state their disposition. Leaving executable-looking, unmarked instructions beside the authoritative V2 docs does not satisfy Q5's final pass over `context/`.

### ADVISORY

- `tests/routes.spec.js:62-77` currently loads real font faces successfully, and direct response inspection confirms the four required files return `200` with `font/woff2`. However, the test discards the array returned by `document.fonts.load(...)`; `document.fonts.check(...)` also returns `true` for an entirely undeclared family (a probe with `DefinitelyMissingQ5` returned zero loaded faces and `true`). Assert that each load resolves at least one face, or inspect the required font responses, so deleting an `@font-face` declaration cannot pass vacuously.
- `tests/routes.spec.js:30-59` verifies that markers with an existing local target are linked, but ignores an orphan bare marker whose target does not exist. The current independent inventory is clean at 372 links and 359 targets, so this does not block Q5; checking every eligible bare `[N]` against a target would preserve that result against future citation drift.
- `npm run lint` remains red with 42 errors and 204 warnings, even though CI deliberately treats lint as advisory. The Chapter 22 `no-useless-escape` errors are useful evidence for REQUIRED 1; after those are fixed, record the remaining lint state in the audit or narrow the configuration so “lint-as-warnings” accurately describes the repository's intended signal.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED finding from critique rounds 1, 2, and 3 against the final artifacts. Round 1's per-chapter widget metadata, linked citation markers, corrected audit evidence, and explicit routing description still hold. Round 2's emitted production fonts and retirement of `DESIGN_SYSTEM.md` and `WIDGET_SPEC.md` still hold. Round 3's repaired equations, final-build Lighthouse record, and V1 context-artifact disposition now hold.

1. Corrected the Chapter 7 contextual-embedding display so its `\\text{...}` labels no longer contain raw underscores. Corrected every malformed Chapter 22 display and inline LaTeX escape, including perplexity, pass@k, judge, and agent-score notation. The production browser sweep now reports zero `.katex-error` nodes across all 25 chapters, and the route smoke asserts that condition for every chapter route.
2. Strengthened the Q5 browser audit: it rejects every eligible bare citation marker, requires each required production font descriptor to resolve to at least one loaded face, and retains the broken-target check. The final route pass succeeds for all 27 production routes.
3. Re-captured the required Lighthouse 12.8.2 mobile baseline against the final local production preview at `2026-07-16T03:26:23.052Z`: Performance 94, Accessibility 100, Best Practices 100, and SEO 100. `audit-report.md` now records its matching 2.3 s FCP/LCP/Speed Index/TTI, 0 ms TBT, 0.076 CLS, 900 ms render-blocking estimate, and 42 KiB unused-JavaScript estimate.
4. Removed the four obsolete V1 artifacts named in round 3 (`CURRICULUM_VISUAL.html`, `MOCKUP_SHELL.html`, `REFERENCE_WIDGET.html`, and `darvinyi_textbook_OpenCode_Guide.docx`) rather than preserving contradictory executable-looking guidance. `PROJECT_OVERVIEW.md` and `audit-report.md` record their removal and point maintainers to the current V2 documents and live source.

Took all three round 3 advisories. `npm run lint` now reports 6 errors and 204 warnings, all outside the repaired equations; it remains explicitly advisory. `npm run check` passes: the build succeeds and all 27 Playwright production-route cases pass.

## Critique round 4 (2026-07-15)

Independent re-review: read `AGENTS.md`, the complete queue and Q5 specification, the relevant Phase Q, Editorial Standard, convention, verification, and Appendix B sections of `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, every current tracked document under `context/`, the complete append-only Q5 critique and git history, all four Q5 implementation commits, and the current audit, routing, test, citation, font, and repaired-equation artifacts. Re-verified every prior REQUIRED fix from the current artifacts: all 25 metadata widget totals agree with 123 rendered widgets; the isolated production route sweep finds no eligible bare or broken citation marker and no `.katex-error` node; the routing description and Lighthouse command are accurate; Vite emits 77 font files and the browser loads each required face; the retired documents are clearly marked; and the four obsolete V1 context artifacts are absent. An independent Lighthouse 12.8.2 mobile run against the current isolated production preview reproduced the recorded 94 Performance, 100 Accessibility, 100 Best Practices, and 100 SEO scores, plus 2.3 s FCP/LCP/Speed Index/TTI, 0 ms TBT, 0.076 CLS, 900 ms render-blocking estimate, and 42 KiB unused-JavaScript estimate. Spot-checked the repaired Chapter 22 pass@k formula against Chen et al. (2021), *Evaluating Large Language Models Trained on Code* (`arXiv:2107.03374`, equation 1), and independently recomputed the chapter's `n=20`, `c=3`, `k=10` example as 0.8947368; both hold. The first sandboxed `npm run check` could not bind a preview port. The required unrestricted invocation then failed for the repository defect below; after isolating the same build and suite on port 4174, all 27 cases passed.

### REQUIRED

1. **`playwright.config.js:10,20-22` and the required `npm run check` gate — local smoke tests can silently attach to an unrelated application instead of this production build.** Port 4173 was already serving another project, “Agentic Loops.” Because `reuseExistingServer: !process.env.CI` accepts any responder at that URL, the unrestricted `npm run check` reused it: the home test received the wrong title and the chapter tests received that project's 404 page, so the command exited nonzero. The identical current build and all 27 Q5 route cases pass when run against an isolated preview on port 4174, which confirms that the content fixes hold but does not make the official gate truthful. Isolate the smoke server for every `npm run check` invocation (for example, a dedicated or configurable port with `reuseExistingServer: false`) so the mandated command can neither test a foreign site nor fail because it selected one; then rerun the unmodified gate successfully.

### ADVISORY

- `eslint.config.js:8` ignores `dist` but not Playwright's ignored output directories. After the failed route run retained trace JavaScript under `playwright-report/`, `npm run lint` scanned generated vendor assets and reported 648 errors plus 204 warnings, while linting tracked JS/JSX alone reproduces the audit's 6 errors and 204 warnings. Add `playwright-report` and `test-results` to `globalIgnores` so the advisory lint signal stays stable after a smoke failure.
- `context/PROJECT_OVERVIEW.md:68-71` lists a nonexistent `tailwind.config.js`, and `README.md:13` still says “remaining V2 work” although Q5 is the final queue item. These are small stale-description remnants; remove or refresh them during the gate fix, but they do not misdescribe reader-facing behavior.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED finding from critique rounds 1 through 4 against the current production artifacts. Round 1's 25 per-chapter widget totals, linked citation markers, corrected audit evidence, and explicit route description remain covered by the 27-route smoke suite and current documents. Round 2's Vite-emitted fonts and retirement of `DESIGN_SYSTEM.md` and `WIDGET_SPEC.md` remain in place. Round 3's repaired Chapter 7/22 notation, zero KaTeX-error assertion, final-build Lighthouse baseline, and removal of the four V1 context artifacts still hold. Round 4's isolated-server requirement now holds: the unmodified gate starts and owns the production preview before running the suite.

1. Updated `playwright.config.js` to use a dedicated `127.0.0.1:4174` preview URL with `--strictPort` and `reuseExistingServer: false`. Playwright can no longer attach the smoke tests to an unrelated responder on the default preview port. The unmodified `npm run check` now builds and passes all 27 production-route cases against that owned preview.
2. Recorded the isolated-preview behavior in `audit-report.md`, so the final audit describes the route gate that actually produced its current evidence.
3. Took both round 4 advisories: `eslint.config.js` now ignores generated `playwright-report` and `test-results` directories, keeping the advisory lint signal stable after a test failure; `context/PROJECT_OVERVIEW.md` no longer lists nonexistent `tailwind.config.js`; and `README.md` now identifies the queue as the completed V2 delivery record. `npm run lint` remains advisory and reports the documented 6 errors and 204 warnings, without generated-report noise.

`npm run check` passes after these changes. The queue row remains `DRAFT`; only Sol may advance it to `DONE`.

## Critique round 5 (2026-07-15)

Independent re-review: read `AGENTS.md`, the complete queue and Q5 specification, the relevant Phase Q and Appendix B material in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, every current tracked document under `context/`, the complete append-only Q5 critique and git history, all five Q5 implementation commits, and every current Q5 audit, routing, citation, font, equation, metadata, and test artifact. Re-verified the prior REQUIRED fixes from the current production artifacts: the unrestricted `npm run check` passes with all 27 route cases; 25 per-chapter metadata totals agree with 123 rendered widgets; the production inventory finds 97 live accessible diagrams, 372 citation links on direct chapter loads, 107 rendered range controls with no unnamed control, and zero `.katex-error` nodes; Vite emits and loads the required font faces; the stale context artifacts remain retired or absent; and the isolated smoke server owns port 4174. An independent Lighthouse 12.8.2 mobile run at `2026-07-16T03:47:16.318Z` exactly reproduces the recorded 94 Performance, 100 Accessibility, 100 Best Practices, and 100 SEO scores, with 2.3 s FCP/LCP/Speed Index/TTI, 0 ms TBT, 0.076 CLS, 900 ms render-blocking estimate, and 42 KiB unused-JavaScript estimate. Spot-checked the repaired Chapter 22 pass@k estimator against Chen et al. (2021), *Evaluating Large Language Models Trained on Code* (`arXiv:2107.03374`), and recomputed the displayed `n=20`, `c=3`, `k=10` example as 0.8947368. The initial sandboxed gate failed only because that environment cannot bind a local port; the required unrestricted gate is green.

### REQUIRED

1. **`src/components/shared/CitationLinkifier.jsx:80-97` and `tests/routes.spec.js:83-94` — citation markers stop being linked after client-side chapter navigation.** This does not reopen the settled direct-load result from critique round 1: every chapter still linkifies correctly when loaded independently. The newly exercised SPA transition path fails because the linkifier effect has an empty dependency array and either returns immediately when the first chapter already has its citation list or disconnects its observer once that list arrives. React reuses the same `ErrorBoundary` / `Suspense` / `CitationLinkifier` wrapper shape when a reader follows the in-app previous/next links, so the effect does not run for the next chapter. In a production-browser reproduction, a direct load of `/ch/01` produced four `a.citation-marker` links; clicking the existing `/ch/02` navigation link reached Chapter 2 with zero citation links and five bare markers (`[4]`, `[2]`, `[1]`, `[5]`, `[3]`). Make the transformation rerun for each chapter pathname (or remount/keep observing the route wrapper), and add a smoke case that follows at least one React Router link before asserting the destination's citation markers. The final audit cannot claim a consistent linked citation path while the book's normal in-app reading flow silently loses it.

### ADVISORY

- None. The current Lighthouse record, global counts, production fonts, equation checks, isolated smoke server, and retired-document disposition all reproduce as documented.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED finding from critique rounds 1 through 5 against the current production artifacts. Round 1's 25 per-chapter widget metadata totals, direct-load citation links and targets, corrected audit evidence, and explicit routing description remain covered. Round 2's emitted and loaded production fonts plus retired pre-V2 documents remain correct. Round 3's Chapter 7/22 equations, zero KaTeX-error check, final-build Lighthouse baseline, and V1-artifact disposition remain correct. Round 4's isolated `4174` smoke server remains owned by Playwright. Round 5's normal in-app chapter transition now re-runs citation linking before the destination route is audited.

1. Updated `src/components/shared/CitationLinkifier.jsx` to depend on React Router's `pathname`. Its DOM transformation and temporary lazy-render observer now run for each chapter route, including when React retains the route-wrapper component during an in-app transition.
2. Added a production smoke case in `tests/routes.spec.js` that loads Chapter 1, checks its linked citation markers, activates the existing `Next: Statistical Learning` navigation button, then checks the Chapter 2 markers and targets with no console errors. This directly covers the failed reader path from round 5.

No advisories were raised in round 5. `npm run check` passes with the production build and all 28 Playwright cases. The queue row remains `DRAFT`; only Sol may advance it to `DONE`.

## Critique round 6 (2026-07-15)

Independent re-review: read `AGENTS.md`, the complete queue and Q5 specification, the relevant Phase Q, Editorial Standard, convention, verification, and Appendix B sections of `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, every current tracked document under `context/`, the complete append-only Q5 critique and git history, all six Q5 implementation commits, and every current Q5 audit, routing, citation, font, equation, metadata, and test artifact. Re-verified the prior REQUIRED fixes from current artifacts: the unrestricted `npm run check` passes with all 28 Playwright cases; the static inventory finds 25 live chapters, 123 page-level `tryThis` props, 123 live widgets plus one parked widget, 97 live accessible diagrams plus one parked diagram, 86 source range controls, and no double-bracket markers, old `Ch`/`Chs` cross-references, or unfinished placeholders; `npm run lint` reproduces the documented 6 errors and 204 warnings; and the retired context artifacts remain clearly retired or absent. An independent Lighthouse 12.8.2 mobile run at `2026-07-16T04:00:09.211Z` reproduces 94 Performance, 100 Accessibility, 100 Best Practices, and 100 SEO, with 2.3 s FCP/LCP/Speed Index/TTI, 0 ms TBT, 0.076 CLS, 900 ms render-blocking estimate, and 42,587 bytes of unused JavaScript. Rechecked the Chapter 22 pass@k estimator against Chen et al. (2021), *Evaluating Large Language Models Trained on Code* (`arXiv:2107.03374`, equation 1), and independently recomputed the displayed `n=20`, `c=3`, `k=10` example as 0.8947368. The round 5 implementation links Chapter 2 correctly once that destination has rendered, but its claimed regression test does not prove that behavior.

### REQUIRED

1. **`tests/routes.spec.js:97-108` — the round 5 regression test audits the outgoing chapter and can pass without checking the destination citation markers.** After clicking `Next: Statistical Learning`, the test waits for the `/ch/02` URL but then asserts only that some `article h1` is visible. The Chapter 1 heading remains visible while the lazy Chapter 2 route is loading, so `expectLinkedCitationMarkers(page)` can immediately inspect Chapter 1's already-linked DOM. Repeating the test's exact synchronization ten times produced the Chapter 1 heading and its four citation links in all ten runs after the URL had already changed to `/ch/02`. This means the test would not catch a regression of the Chapter 2 linkification path required by critique round 5. Wait for the destination artifact itself, such as the exact `Statistical Learning` heading or destination document title, before asserting Chapter 2's linked markers and targets. The current implementation produced five linked Chapter 2 markers and no eligible bare markers when checked after that exact destination wait, so this finding is about the still-unmet regression-evidence requirement, not a reader-visible failure in the present build.

### ADVISORY

- None. The current implementation behavior, global inventories, Lighthouse baseline, production fonts, equation checks, isolated smoke server, and retired-document disposition otherwise reproduce as documented.

## Builder resolution (2026-07-16)

Regression gate: re-verified every REQUIRED finding from critique rounds 1 through 6 against the current Q5 artifacts. Round 1's chapter widget metadata, direct-load citation links and targets, audit evidence, and routing description remain covered by the production route suite and current documentation. Round 2's Vite-emitted production fonts and explicit retirement of pre-V2 specifications still hold. Round 3's corrected Chapter 7 and Chapter 22 notation, zero KaTeX-error assertion, final-build Lighthouse record, and obsolete-context-artifact disposition remain in place. Round 4's isolated, Playwright-owned `127.0.0.1:4174` preview continues to prevent smoke tests from attaching to another application. Round 5's pathname-aware linkifier continues to link citation markers after in-app Chapter 1 to Chapter 2 navigation. Round 6's regression evidence now waits for the actual Chapter 2 artifact before inspecting the destination markers.

1. Updated `tests/routes.spec.js` so the SPA navigation regression test waits for the exact, visible `Statistical Learning` heading after reaching `/ch/02`. The test can no longer pass by inspecting the outgoing Chapter 1 DOM while the lazy destination route is still rendering; only then does it check Chapter 2's citation links and targets.

No advisories were raised in round 6. `npm run check` passes with the production build and all 28 Playwright cases. The Q5 queue row remains `DRAFT`; only Sol may advance it to `DONE`.
