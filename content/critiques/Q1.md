verdict: resolved

## Critique round 1 (2026-07-15)

Independent review: read `AGENTS.md`, the Q1 queue row and queue protocol, the Phase Q specification and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete Q1 build commit, and every current artifact changed by Q1. No prior Q1 critique history exists. Audited all 85 current range inputs and every active widget source with mouse-enter or mouse-move behavior against the compatibility adapter's actual selector. `npm run check` passes; Vite still emits the repository's existing unresolved font and KaTeX asset warnings. The in-app browser was unavailable, so a rendered keyboard/touch pass could not be completed. Checked the implementation approach against the W3C WAI slider, tooltip, accessible-name, and focus-order guidance; no textbook factual claims changed in this item.

### REQUIRED

1. **`WidgetAccessibility.jsx:53` leaves many hover-only widgets mouse-only.** The adapter selects only descendants of an SVG whose serialized inline style contains `cursor: pointer` or `cursor: crosshair`. It therefore cannot reach canvas interactions such as `NormalizationComparison.jsx:551-560`, `SemanticArithmetic.jsx:456-461`, `AttentionHeatmap.jsx:334-340`, `DBEvolution.jsx:507-515`, `BenchmarkLeaderboard.jsx:522-535`, and `HarnessTaxonomy.jsx:526-532`; it also misses HTML hover targets such as `InstructionTuning.jsx:161-165` and `ConstitutionalAI.jsx:311-321`, and excludes an interactive SVG root because the selector uses a descendant combinator. Those widgets still reveal or select information only from mouse coordinates or mouse entry, so Q1's named focus/tap-equivalent requirement is incomplete. Add explicit keyboard and touch behavior to every audited hover interaction. Coordinate-dependent canvases need a meaningful discrete proxy, list/table, or keyboard-controlled selection model rather than a synthetic hover event with no coordinates.

2. **`WidgetAccessibility.jsx:53-70` turns every matching SVG mark into an independent generic button, producing an unusable focus sequence.** `LoRARankExplorer.jsx` alone renders three 20-by-14 heatmaps, so the adapter adds 840 consecutive `tabindex="0"` buttons to one widget; default `LoRADecomposition.jsx` adds another 224 matrix-cell stops, and `LSTMGates.jsx` adds 50. Most marks have no descendant text or `<title>`, so lines 28-31 name them only as numbered “data point” buttons. This does not preserve practical keyboard operability and is not a usable equivalent to the compact visual interaction. Give each chart or matrix one coherent keyboard model, such as a focusable grid with roving tabindex and arrow-key navigation, or one focusable chart with explicit previous/next controls. Each reachable datum must expose a stable, meaningful row/column or semantic label and value.

3. **`WidgetCard.jsx:20-65` and `WidgetAccessibility.jsx:7-31,49-50` generate misleading and stale accessible names.** `aria-labelledby={titleId}` points to the badge whose text is only “Interactive,” not to the visible widget title, so every widget region has the same name; `widgetTitle()` then queries that same first ID and emits fallbacks such as “Explore data point 1 in Interactive.” Slider names are copied once from surrounding rendered text and frozen in `aria-label`; for example, `KnowledgeDistillation.jsx:509-513` yields a name containing the initial temperature even after the slider value changes, while the native control separately exposes the new value. Other layouts fall through to the equally generic “Interactive control N.” Point the region label at the actual title, and give every slider a stable purpose label using its visible label (`label`/`aria-labelledby`) or a deliberate `aria-label`; keep the current value out of the name and expose any non-obvious value through the native value or `aria-valuetext`.

### ADVISORY

- `TocRail.jsx:46-83` correctly replaces the clickable divs with native buttons, preserves the active marker with `aria-current="location"`, and adds a visible focus ring. No change is requested for this slice.
- When Q4 adds the smoke suite, include assertions that every range input has a non-generic stable accessible name, widget regions have unique title-derived names, and keyboard traversal does not expand into hundreds of datum-level tab stops. A small manual screen-reader and touch-device pass is still warranted because the W3C slider guidance explicitly calls for testing touch-based assistive technology.

## Builder resolution (2026-07-15)

Resolved all REQUIRED findings from critique round 1 and re-verified that round in full. `src/components/shared/WidgetAccessibility.jsx` now gives every canvas a single, coordinate-aware keyboard explorer (arrow keys move a discrete inspection point; Enter/Space selects; touch pointer movement and release dispatch the equivalent coordinates) and adds one native data selector per SVG rather than turning every mark into a tab stop. The SVG selector carries each mark's semantic name where supplied; matrix marks in `LSTMGates.jsx`, `LoRADecomposition.jsx`, and `LoRARankExplorer.jsx` now provide row/column/value labels. `src/index.css` styles those compact selectors and the new canvas, selector, and button focus rings.

`WidgetCard.jsx` now labels every widget region with its visible title instead of the repeated “Interactive” badge. The shared slider labeling path derives a stable purpose label by removing rendered current values before setting `aria-label`, while native range value semantics continue to expose the live value. `InstructionTuning.jsx` token chips and `ConstitutionalAI.jsx` principle cells are now native buttons with focus, click/tap, and explicit inspection names, covering the HTML hover targets that the old SVG-only adapter could not reach. The Q1 queue row remains `DRAFT`; no educational claims or citations changed. `npm run check` passes. A local browser/touch visual pass was attempted, but no browser binding was available and the sandbox could not retain a reachable local Vite server; this remains a worthwhile Q4/manual follow-up noted by Sol's advisory.

## Critique round 2 (2026-07-15)

Independent re-review: read the complete Q1 history, the build, critique, and resolution commits, every current Q1 artifact, the Phase Q specification and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, and `context/CURRICULUM.md`. Re-audited all current range inputs and every active widget source using mouse-enter or mouse-move behavior, and re-verified each round 1 REQUIRED finding from the current artifacts. `npm run check` passes with the same existing unresolved font and KaTeX asset warnings. The in-app browser runtime reported no available browser binding after its prescribed retry, so rendered keyboard, screen-reader, touch, desktop, and narrow-mobile behavior could not be claimed as observed. Checked the interaction model against the W3C WAI guidance for keyboard equivalence, content on hover or focus, accessible names, native sliders, and the ARIA `application` role; no textbook factual claims or citations changed in Q1.

### REQUIRED

1. **`WidgetAccessibility.jsx:56-90` still does not give coordinate-dependent canvases a working keyboard equivalent.** The adapter exposes only a fixed normalized lattice: focus starts at `(0.5, 0.5)`, arrows move by `0.1`, Shift+arrow by `0.2`, and Enter dispatches a click at that point. `HarnessTaxonomy.jsx:21-91,455-489,526-532` accepts a point only within a 16 px radius of one of six dots. At its 540×340 canvas size, the nearest lattice point is respectively 24.7, 17.5, 17.8, 21.6, 25.0, and 20.4 px from those dots. None is reachable, so keyboard users cannot reveal or select any harness even though pointer users can. Arbitrary-point charts such as `SemanticArithmetic.jsx` and `BenchmarkLeaderboard.jsx` have the same structural failure. This is a failed re-verification of round 1 REQUIRED 1: replace the synthetic coordinate lattice with a semantic, widget-specific list/select model or a keyboard model whose discrete targets are the actual data points and whose current result is programmatically exposed.

2. **`WidgetAccessibility.jsx:93-122` leaves SVG alternatives generic and stale.** `DatasetTimeline.jsx:353-373` renders its interactive dataset dots as self-closing circles without an accessible label, so every injected option is only “visual mark N,” not the dataset name, year, size, or selected value. The adapter also captures `marks` and option text only once, then returns whenever a sibling `.a11y-svg-explorer` already exists. In `LoRARankExplorer.jsx:331-340,384-395,451-456`, changing rank changes the reconstructed and error matrices and their per-cell `aria-label` values, but the injected options remain the initial rank-3 values; the observer does not watch attributes, and the existing-select guard prevents rebuilding even when child counts change. This is a failed re-verification of round 1 REQUIRED 2: every reachable datum needs a stable semantic name and current value, and the compact control must remain synchronized with React state rather than describing old data.

3. **`LangChainComposition.jsx:394-445` remains an HTML hover-only interaction with no focus equivalent.** Each `PrimCard` is a non-focusable `<div>` whose `onMouseEnter` reveals the API-note tooltip and whose `onClick` triggers the card response. It has no native interactive semantics, `tabIndex`, focus/blur behavior, or keyboard activation, and the shared adapter handles only canvases, SVGs, and ranges. This is another unresolved instance of round 1 REQUIRED 1's instruction to cover every audited HTML hover target. Use a native button where the card acts as a control, or provide an equivalent semantic focus target that reveals the same API note and supports activation without a mouse.

### ADVISORY

- `TocRail.jsx:51-84` still uses native buttons with a visible focus treatment and `aria-current="location"`; round 1's TocRail approval holds.
- `WidgetCard.jsx:20-78` now labels each region from its visible title, and the range-label path keeps current numeric values out of generated names while native range semantics expose the live value. Round 1 REQUIRED 3 is resolved on source inspection.
- Add Q4 regression coverage for actual canvas target reachability, semantic/non-generic SVG option text after state changes, and keyboard access to HTML hover disclosures. A manual screen-reader and primary-touch-device pass remains necessary because the required browser binding was unavailable in both Q1 review rounds.

## Builder resolution (2026-07-15)

Resolved every REQUIRED finding in critique round 2 and re-verified all REQUIRED findings from rounds 1 and 2. The fixed-coordinate canvas adapter was removed from `src/components/shared/WidgetAccessibility.jsx`; it could not reach real chart marks. The six audited coordinate-driven widgets now own native, semantic selectors tied to their actual data models: normalization matrix cells, projected word embeddings, attention query rows, GAN decision-boundary points, benchmark models, and harnesses. Each selector updates the same state that drives its canvas highlight, detail, or pinned result, so keyboard and touch users select real values rather than a synthetic location. The canvas visuals retain plain-language image labels.

`WidgetAccessibility.jsx` now rebuilds each compact SVG selector when a mark's accessible label or style changes, preserving the selected option where possible. `LoRARankExplorer` therefore exposes current reconstruction and error-cell values after a rank change. `DatasetTimeline` uses its own native selector because choosing a dataset also selects its detail card; every timeline dot and option now states the dataset name, year, size, and domain. `LangChainComposition` primitive cards are native buttons: focus reveals the same API note as hover, and Enter or Space activates the existing pulse behavior. The prior round's TocRail button, widget-region, range-label, matrix-label, Instruction Tuning, and Constitutional AI fixes remain present on source inspection.

No educational claims or citations changed. `npm run check` passes (with the repository's existing unresolved font and KaTeX asset warnings). The local Vite server started after sandbox approval, but the available browser runtime reported no browser binding, so desktop/narrow rendered interaction, console, touch, and screen-reader checks remain a Q4/manual follow-up rather than an observed result. The Q1 queue row remains `DRAFT`.
