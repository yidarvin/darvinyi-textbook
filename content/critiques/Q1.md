verdict: revise

## Critique round 1 (2026-07-15)

Independent review: read `AGENTS.md`, the Q1 queue row and queue protocol, the Phase Q specification and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete Q1 build commit, and every current artifact changed by Q1. No prior Q1 critique history exists. Audited all 85 current range inputs and every active widget source with mouse-enter or mouse-move behavior against the compatibility adapter's actual selector. `npm run check` passes; Vite still emits the repository's existing unresolved font and KaTeX asset warnings. The in-app browser was unavailable, so a rendered keyboard/touch pass could not be completed. Checked the implementation approach against the W3C WAI slider, tooltip, accessible-name, and focus-order guidance; no textbook factual claims changed in this item.

### REQUIRED

1. **`WidgetAccessibility.jsx:53` leaves many hover-only widgets mouse-only.** The adapter selects only descendants of an SVG whose serialized inline style contains `cursor: pointer` or `cursor: crosshair`. It therefore cannot reach canvas interactions such as `NormalizationComparison.jsx:551-560`, `SemanticArithmetic.jsx:456-461`, `AttentionHeatmap.jsx:334-340`, `DBEvolution.jsx:507-515`, `BenchmarkLeaderboard.jsx:522-535`, and `HarnessTaxonomy.jsx:526-532`; it also misses HTML hover targets such as `InstructionTuning.jsx:161-165` and `ConstitutionalAI.jsx:311-321`, and excludes an interactive SVG root because the selector uses a descendant combinator. Those widgets still reveal or select information only from mouse coordinates or mouse entry, so Q1's named focus/tap-equivalent requirement is incomplete. Add explicit keyboard and touch behavior to every audited hover interaction. Coordinate-dependent canvases need a meaningful discrete proxy, list/table, or keyboard-controlled selection model rather than a synthetic hover event with no coordinates.

2. **`WidgetAccessibility.jsx:53-70` turns every matching SVG mark into an independent generic button, producing an unusable focus sequence.** `LoRARankExplorer.jsx` alone renders three 20-by-14 heatmaps, so the adapter adds 840 consecutive `tabindex="0"` buttons to one widget; default `LoRADecomposition.jsx` adds another 224 matrix-cell stops, and `LSTMGates.jsx` adds 50. Most marks have no descendant text or `<title>`, so lines 28-31 name them only as numbered “data point” buttons. This does not preserve practical keyboard operability and is not a usable equivalent to the compact visual interaction. Give each chart or matrix one coherent keyboard model, such as a focusable grid with roving tabindex and arrow-key navigation, or one focusable chart with explicit previous/next controls. Each reachable datum must expose a stable, meaningful row/column or semantic label and value.

3. **`WidgetCard.jsx:20-65` and `WidgetAccessibility.jsx:7-31,49-50` generate misleading and stale accessible names.** `aria-labelledby={titleId}` points to the badge whose text is only “Interactive,” not to the visible widget title, so every widget region has the same name; `widgetTitle()` then queries that same first ID and emits fallbacks such as “Explore data point 1 in Interactive.” Slider names are copied once from surrounding rendered text and frozen in `aria-label`; for example, `KnowledgeDistillation.jsx:509-513` yields a name containing the initial temperature even after the slider value changes, while the native control separately exposes the new value. Other layouts fall through to the equally generic “Interactive control N.” Point the region label at the actual title, and give every slider a stable purpose label using its visible label (`label`/`aria-labelledby`) or a deliberate `aria-label`; keep the current value out of the name and expose any non-obvious value through the native value or `aria-valuetext`.

### ADVISORY

- `TocRail.jsx:46-83` correctly replaces the clickable divs with native buttons, preserves the active marker with `aria-current="location"`, and adds a visible focus ring. No change is requested for this slice.
- When Q4 adds the smoke suite, include assertions that every range input has a non-generic stable accessible name, widget regions have unique title-derived names, and keyboard traversal does not expand into hundreds of datum-level tab stops. A small manual screen-reader and touch-device pass is still warranted because the W3C slider guidance explicitly calls for testing touch-based assistive technology.
