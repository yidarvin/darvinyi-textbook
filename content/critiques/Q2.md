verdict: resolved

## Critique round 1 (2026-07-15)

Fresh-eyes review: read `AGENTS.md`, the Q2 Phase Q specification and Appendix B rubric in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete Q2 build diff, and the current `Sidebar`, layout/router, mobile navigation, chapter data, styles, and all 25 page-local `TOC_SECTIONS` arrays. There was no prior Q2 critique history. Ran `npm run check` (passes). Independently compared all 121 indexed section IDs and labels with the page-local TOC data and confirmed that every target ID also appears in its chapter artifact. No factual claims or external sources were introduced by Q2, so no primary-source spot-check was applicable. I attempted the directed desktop/mobile browser check, but no browser backend was available in this environment; interaction conclusions below therefore come from tracing the rendered component and router state paths in source.

### REQUIRED

1. **`src/components/layout/Layout.jsx` / `src/components/layout/Sidebar.jsx` — a same-chapter section search result does not dismiss the mobile drawer.** `SectionMatch` navigates to the current pathname plus `#<section-id>`, while `Layout` closes `MobileSidebarDrawer` only in an effect keyed to `location.pathname`. When a reader opens the chapter menu on mobile, searches for a section in the chapter already open, and activates that result, only `location.hash` changes: the content scrolls behind the still-open modal drawer and the selected section is not surfaced. Close the drawer on hash navigation as well as pathname navigation (or pass an explicit result-selection callback through the drawer/Sidebar), then verify both same-chapter and cross-chapter section results at a narrow viewport.

### ADVISORY

- Q2 copies all 121 page-local `TOC_SECTIONS` entries into `src/data/chapters.js`. The copies match exactly today, but section navigation now has two manually synchronized sources, so a future heading edit can silently make search stale while the on-page TOC remains correct. A later cleanup could extract shared section metadata that both the pages and Sidebar consume, or add a consistency test. This is not blocking because the current index and targets are accurate.

## Builder resolution (2026-07-15)

Regression gate: re-verified every REQUIRED finding from critique round 1 against the current route-state flow. The Q2 queue row remains `DRAFT`; no status transition was made.

1. Updated `src/components/layout/Layout.jsx` so the mobile drawer closes when either `location.pathname` or `location.hash` changes. Selecting a same-chapter section result now dismisses the drawer as `scrollToHashOrTop` reveals that target; cross-chapter results retain the existing pathname-driven close behavior.

The round 1 metadata-duplication note remains advisory and was not taken, to avoid an unrelated search-index rewrite. `npm run check` passes. I started the local Vite server for the requested narrow-viewport check, but no browser backend is available in this environment, so the same-chapter and cross-chapter interactions could not be exercised visually here.

## Critique round 2 (2026-07-15)

Independent re-review: read `AGENTS.md`, the complete queue and Q2 specification, the relevant Phase Q and Appendix B material in `context/V2_PLAN.md`, `context/STYLE_GUIDE.md`, `context/CURRICULUM.md`, the complete append-only Q2 critique and git history, the cumulative Q2 diff, and the current `Sidebar`, layout/router, mobile navigation, chapter data, styles, and all 25 page-local `TOC_SECTIONS` arrays. Re-verified round 1's REQUIRED finding against the current route-state flow. `npm run check` passes with the repository's existing unresolved font and KaTeX asset warnings. A fresh data check found 128 indexed sections, confirmed exact metadata agreement with every page-local TOC array, and confirmed that all 128 IDs exist as targets in their chapter sources. The local Vite server started, but the browser runtime exposed no browser backend after its prescribed availability check, so this round does not claim rendered desktop, narrow-mobile, keyboard, screen-reader, touch, or console observation. No textbook factual claims or citations changed in Q2, so no subject-matter primary-source spot-check was applicable; the accessibility semantics below were checked against the installed React Router 7.14.2 implementation and the W3C WAI-ARIA specification.

### REQUIRED

1. **`src/components/layout/Sidebar.jsx:134-157` marks every matching section in the open chapter as the current page, regardless of its hash.** `SectionMatch` computes the correct hash-sensitive `isActive` value for styling, but renders a `NavLink`. React Router 7.14.2 decides a `NavLink`'s built-in active state from the pathname only, so at `/ch/19#mode-collapse`, links to both `/ch/19#training-dynamics` and `/ch/19#mode-collapse` render with `class="active"` and `aria-current="page"`; a direct server render reproduced exactly that output. This also marks a single non-selected result current whenever it belongs to the open chapter. WAI-ARIA defines `aria-current` as the current item within a related set and says authors should mark only one element in that set ([WAI-ARIA 1.2, `aria-current`](https://www.w3.org/TR/wai-aria-1.2/#aria-current)). Use a plain `Link` with the existing hash-sensitive state, or otherwise suppress `NavLink`'s pathname-only current semantics for non-selected hashes and expose only the exact hash match as `aria-current="location"`.

### ADVISORY

- Round 1's mobile-drawer finding is resolved for navigation that changes `location.pathname` or `location.hash`: the new `Layout.jsx:198-208` dependencies close the drawer and run the hash scroll path. If a reader activates the already-current section result, however, neither dependency changes, so the drawer remains open. An explicit result-selection callback would cover that exact-target edge case as well; this is non-blocking because ordinary same-chapter and cross-chapter selections now work by source tracing.
- The section index currently matches all 128 page-local TOC entries and targets exactly. Round 1's duplicated-metadata advisory still stands; the historical count of 121 in that round was low, but its synchronization concern is unchanged. Q4 could add the consistency check used in this review so future section edits cannot silently stale the search index.

## Builder resolution (2026-07-15)

Regression gate: re-verified the REQUIRED findings from critique rounds 1 and 2 against the current route-state flow and rendered-link implementation. Round 1 remains fixed: `Layout.jsx` closes the mobile drawer whenever either `location.pathname` or `location.hash` changes, so same-chapter and cross-chapter section selections with a changed target dismiss the drawer before `scrollToHashOrTop` reveals the target. The Q2 queue row remains `DRAFT`; no status transition was made.

1. Updated `src/components/layout/Sidebar.jsx` so `SectionMatch` uses React Router's plain `Link`, not `NavLink`. The existing hash-sensitive `isActive` condition continues to drive the selected-result style, and only its exact `pathname` plus `#section-id` match receives `aria-current="location"`. Other matching sections in the same chapter no longer inherit React Router's pathname-only `active` class or `aria-current="page"` semantics.

Neither carried-forward advisory was taken: an explicit already-current-result callback and a shared section-metadata source would be unrelated to this focused required fix. `npm run check` passes with the repository's existing unresolved font and KaTeX asset warnings. I attempted the requested narrow-viewport interaction check through the available browser runtime, but it reported that no browser was available; no visual interaction or console observation is claimed here.
